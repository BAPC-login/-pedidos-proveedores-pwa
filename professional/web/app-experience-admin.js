import {$,$$,esc,state,api,toast} from './app-core.js';
import {openModal} from './app-modal.js';
import {bindDynamic} from './app-actions.js';
import {openProcurementSettings} from './app-procurement-settings.js';
import {hydrateProtectedImages,protectedAssetUrl} from './app-assets-v13.js';

let busy=false,activeTab='categories';
const normalize=value=>String(value||'').normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLowerCase();

function setActive(){$$('.nav-item[data-view],.bottom-item[data-view],.nav-item[data-experience-view],.bottom-item[data-experience-view]').forEach(button=>button.classList.toggle('active',(button.dataset.experienceView||button.dataset.view)==='operations'));$('#pageEyebrow').textContent='MAESTROS';$('#pageTitle').textContent='Operaciones'}

function imageToJpeg(file){return new Promise((resolve,reject)=>{const source=URL.createObjectURL(file),image=new Image();image.onload=()=>{try{const limit=1400,scale=Math.min(1,limit/Math.max(image.naturalWidth||1,image.naturalHeight||1)),width=Math.max(1,Math.round(image.naturalWidth*scale)),height=Math.max(1,Math.round(image.naturalHeight*scale)),canvas=document.createElement('canvas');canvas.width=width;canvas.height=height;const context=canvas.getContext('2d');context.fillStyle='#fff';context.fillRect(0,0,width,height);context.drawImage(image,0,0,width,height);canvas.toBlob(blob=>{URL.revokeObjectURL(source);if(!blob)return reject(new Error('No se pudo procesar el logo'));resolve({blob,width,height})},'image/jpeg',.92)}catch(error){URL.revokeObjectURL(source);reject(error)}};image.onerror=()=>{URL.revokeObjectURL(source);reject(new Error('Usa una imagen JPG, PNG o WebP'))};image.src=source})}

async function openCategoryCreator(){openModal({eyebrow:'CATEGORÍAS',title:'Nueva categoría',subtitle:'Solo aparecerán categorías creadas por ti.',body:'<label class="field"><span>Nombre de categoría</span><input name="name" required placeholder="Ej: Cervezas importadas"></label>',submitLabel:'Crear categoría',onSubmit:async form=>{await api('/api/categories',{method:'POST',json:{name:form.get('name')}});toast('Categoría creada');activeTab='categories';await renderOperationsAdmin()}})}
async function renameCategory(category){openModal({eyebrow:'CATEGORÍAS',title:'Editar categoría',subtitle:'El cambio se aplicará a productos y listas.',body:`<label class="field"><span>Nombre</span><input name="name" value="${esc(category.name)}" required></label>`,submitLabel:'Guardar nombre',onSubmit:async form=>{await api(`/api/categories/${category.id}`,{method:'PATCH',json:{name:form.get('name')}});toast('Categoría actualizada');await renderOperationsAdmin()}})}

async function uploadSupplierLogo(supplier,asset={}){
  let pending=null,preview=asset.logoKey?await protectedAssetUrl(asset.logoKey):'';
  openModal({eyebrow:'LOGO DE PROVEEDOR',title:supplier.name,subtitle:'Se mostrará en la lista, pedidos emitidos y PDF.',body:`<div class="supplier-logo-editor"><div id="supplierLogoPreview">${preview?`<img src="${preview}" alt="Logo ${esc(supplier.name)}">`:'<span>Sin logo</span>'}</div><label class="field"><span>Seleccionar logo</span><input id="supplierLogoFile" type="file" accept="image/jpeg,image/png,image/webp"></label><label class="field"><span>Tamaño en PDF</span><input name="logoSize" type="range" min="24" max="96" value="${Number(asset.logoSize||44)}"></label><label class="check-card"><input name="removeLogo" type="checkbox"><span><strong>Quitar logo actual</strong><small>No modifica documentos históricos.</small></span></label></div>`,submitLabel:'Guardar logo',onSubmit:async form=>{let logo={logoKey:asset.logoKey||'',logoName:asset.logoName||'',logoWidth:asset.logoWidth||0,logoHeight:asset.logoHeight||0,logoSize:Number(form.get('logoSize')||44)};if(form.get('removeLogo')==='on')logo={...logo,logoKey:'',logoName:'',logoWidth:0,logoHeight:0};if(pending){const converted=await imageToJpeg(pending),upload=new FormData();upload.append('file',converted.blob,`${String(pending.name||'logo').replace(/\.[^.]+$/,'')}.jpg`);const response=await fetch('/api/files?purpose=supplier-logo',{method:'POST',headers:{Authorization:`Bearer ${state.token}`},body:upload}),payload=await response.json().catch(()=>({}));if(!response.ok||payload.ok===false)throw new Error(payload.error||'No se pudo cargar el logo');logo={logoKey:payload.file.key,logoName:payload.file.name,logoWidth:converted.width,logoHeight:converted.height,logoSize:Number(form.get('logoSize')||44)}}await api(`/api/suppliers/${supplier.id}/identity`,{method:'PATCH',json:logo});toast('Logo guardado');activeTab='suppliers';await renderOperationsAdmin()}});
  $('#supplierLogoFile').onchange=async()=>{pending=$('#supplierLogoFile').files?.[0]||null;if(!pending)return;const converted=await imageToJpeg(pending);preview=URL.createObjectURL(converted.blob);$('#supplierLogoPreview').innerHTML=`<img src="${preview}" alt="Vista previa">`};
}

function categoryPanel(categories){return `<section class="operations-panel"><div class="settings-section-head"><div><h3>Mis categorías</h3><p>Arrastra conceptualmente con subir/bajar. El orden se guarda inmediatamente.</p></div><button class="btn primary" type="button" data-new-category>＋ Categoría</button></div><div class="category-manager-list">${categories.map((category,index)=>`<article class="category-manager-row" data-category-id="${esc(category.id)}"><span class="category-position">${index+1}</span><div><strong>${esc(category.name)}</strong><small>Usada en tu catálogo y recorrido.</small></div><div class="category-manager-actions"><button class="btn small" type="button" data-category-up="${esc(category.id)}" ${index===0?'disabled':''}>↑</button><button class="btn small" type="button" data-category-down="${esc(category.id)}" ${index===categories.length-1?'disabled':''}>↓</button><button class="btn small" type="button" data-category-edit="${esc(category.id)}">Editar</button><button class="btn small danger" type="button" data-category-delete="${esc(category.id)}">Eliminar</button></div></article>`).join('')||'<div class="empty-state"><h3>No has creado categorías</h3><p>Las categorías automáticas fueron ocultadas. Crea las tuyas para ordenar la lista.</p></div>'}</div></section>`}

function productPanel(products,categories){return `<section class="operations-panel"><div class="settings-section-head"><div><h3>Productos y categorías</h3><p>Asigna cada producto exclusivamente a una categoría creada por ti.</p></div><button class="btn primary" data-action="new-product">＋ Producto</button></div><label class="field"><span>Buscar producto</span><input id="operationsProductSearch" placeholder="Nombre, proveedor o categoría"></label><div class="product-category-list" id="productCategoryList">${products.map(product=>`<article class="product-category-row" data-product-search="${esc(normalize(`${product.name} ${product.categoryName||''} ${(product.suppliers||[]).map(item=>item.supplierName).join(' ')}`))}"><div><strong>${esc(product.name)}</strong><small>${esc((product.suppliers||[]).map(item=>item.supplierName).join(', ')||'Sin proveedor')}</small></div><label><span>Categoría</span><select data-product-category="${esc(product.id)}"><option value="">Sin categoría</option>${categories.map(category=>`<option value="${esc(category.id)}" ${product.categoryId===category.id?'selected':''}>${esc(category.name)}</option>`).join('')}</select></label><button class="btn small" type="button" data-assign-cost-centers="${esc(product.id)}">Centros</button></article>`).join('')}</div></section>`}

function centerPanel(centers){return `<section class="operations-panel"><div class="settings-section-head"><div><h3>Centros, bodegas y recorrido</h3><p>Cada centro conserva sus propias unidades, bodegas y orden.</p></div><button class="btn primary" data-action="new-cost-center">＋ Centro</button></div><div class="ops-master-list">${centers.map(center=>`<article class="ops-master-row"><div><strong>${esc(center.name)}</strong><small>${esc(center.locationName)} · ${center.productCount||0} productos</small></div><button class="btn" type="button" data-center-config="${esc(center.id)}">Configurar recorrido</button></article>`).join('')}</div></section>`}

function supplierLogo(supplier,asset){const initials=String(supplier.name||'?').split(/\s+/).slice(0,2).map(item=>item[0]).join('').toUpperCase();return `<span class="supplier-logo-shell" data-logo-shell>${asset.logoKey?`<img data-protected-key="${esc(asset.logoKey)}" alt="Logo ${esc(supplier.name)}">`:''}<b>${esc(initials)}</b></span>`}
function supplierPanel(suppliers,assets){const map=new Map(assets.map(asset=>[asset.supplierId,asset]));return `<section class="operations-panel"><div class="settings-section-head"><div><h3>Proveedores y logos</h3><p>Los logos cargados se muestran aquí, en pedidos y en los documentos.</p></div><button class="btn primary" data-action="new-supplier">＋ Proveedor</button></div><div class="supplier-logo-grid">${suppliers.map(supplier=>{const asset=map.get(supplier.id)||{};return `<article class="supplier-logo-card">${supplierLogo(supplier,asset)}<div><strong>${esc(supplier.name)}</strong><small>${supplier.productCount||0} productos</small><span>${asset.logoKey?'Logo activo':'Sin logo'}</span></div><button class="btn" type="button" data-supplier-logo="${esc(supplier.id)}">${asset.logoKey?'Cambiar logo':'Cargar logo'}</button></article>`}).join('')}</div></section>`}

export async function renderOperationsAdmin(tab=activeTab){
  if(busy)return;busy=true;activeTab=tab;state.view='operations';setActive();
  if(!$('#operationsAdminV13'))$('#mainContent').innerHTML='<section class="panel"><div class="empty-state compact-empty">Cargando operaciones…</div></section>';
  try{
    const [categoriesPayload,centersPayload,suppliersPayload,productsPayload,assetsPayload]=await Promise.all([api('/api/categories'),api('/api/cost-centers'),api('/api/suppliers'),api('/api/products'),api('/api/supplier-assets')]);
    const categories=categoriesPayload.categories||[],centers=centersPayload.costCenters||[],suppliers=suppliersPayload.suppliers||[],products=productsPayload.products||[],assets=assetsPayload.assets||[];
    state.cache.categories=categories;state.cache.costCenters=centers;state.cache.suppliers=suppliers;state.cache.products=products;
    const panels={categories:categoryPanel(categories),products:productPanel(products,categories),centers:centerPanel(centers),suppliers:supplierPanel(suppliers,assets)};
    $('#mainContent').innerHTML=`<div id="operationsAdminV13"><div class="view-header ops-header"><div><span class="eyebrow">CONFIGURACIÓN OPERATIVA</span><h2>Operaciones</h2><p>Cada menú administra una sola cosa. Nada se mezcla con empresa, apariencia o seguridad.</p></div></div><nav class="operations-tabs"><button class="${activeTab==='categories'?'active':''}" data-operations-tab="categories">Categorías</button><button class="${activeTab==='products'?'active':''}" data-operations-tab="products">Productos</button><button class="${activeTab==='centers'?'active':''}" data-operations-tab="centers">Centros y bodegas</button><button class="${activeTab==='suppliers'?'active':''}" data-operations-tab="suppliers">Proveedores y logos</button></nav>${panels[activeTab]||panels.categories}</div>`;
    bindDynamic();
    $$('[data-operations-tab]').forEach(button=>button.onclick=()=>renderOperationsAdmin(button.dataset.operationsTab));
    $$('[data-new-category]').forEach(button=>button.onclick=()=>openCategoryCreator().catch(error=>toast(error.message,'error')));
    const moveCategory=async(id,direction)=>{const index=categories.findIndex(item=>item.id===id),target=index+direction;if(index<0||target<0||target>=categories.length)return;[categories[index],categories[target]]=[categories[target],categories[index]];await api('/api/categories/order',{method:'PUT',json:{categoryIds:categories.map(item=>item.id)}});toast('Orden guardado');await renderOperationsAdmin('categories')};
    $$('[data-category-up]').forEach(button=>button.onclick=()=>moveCategory(button.dataset.categoryUp,-1).catch(error=>toast(error.message,'error')));
    $$('[data-category-down]').forEach(button=>button.onclick=()=>moveCategory(button.dataset.categoryDown,1).catch(error=>toast(error.message,'error')));
    $$('[data-category-edit]').forEach(button=>button.onclick=()=>renameCategory(categories.find(item=>item.id===button.dataset.categoryEdit)).catch(error=>toast(error.message,'error')));
    $$('[data-category-delete]').forEach(button=>button.onclick=async()=>{const category=categories.find(item=>item.id===button.dataset.categoryDelete);if(!confirm(`¿Eliminar la categoría “${category?.name}”? Los productos quedarán sin categoría.`))return;try{await api(`/api/categories/${button.dataset.categoryDelete}`,{method:'DELETE'});toast('Categoría eliminada');await renderOperationsAdmin('categories')}catch(error){toast(error.message,'error')}});
    $$('[data-product-category]').forEach(select=>select.onchange=async()=>{select.disabled=true;try{await api(`/api/products/${select.dataset.productCategory}/category`,{method:'PATCH',json:{categoryId:select.value}});toast('Categoría del producto guardada')}catch(error){toast(error.message,'error')}finally{select.disabled=false}});
    if($('#operationsProductSearch'))$('#operationsProductSearch').oninput=()=>{const query=normalize($('#operationsProductSearch').value);$$('[data-product-search]').forEach(row=>row.classList.toggle('hidden',query&&!row.dataset.productSearch.includes(query)))};
    $$('[data-center-config]').forEach(button=>button.onclick=()=>openProcurementSettings(button.dataset.centerConfig).catch(error=>toast(error.message,'error')));
    $$('[data-supplier-logo]').forEach(button=>{const supplier=suppliers.find(item=>item.id===button.dataset.supplierLogo),asset=assets.find(item=>item.supplierId===button.dataset.supplierLogo)||{};button.onclick=()=>uploadSupplierLogo(supplier,asset).catch(error=>toast(error.message,'error'))});
    hydrateProtectedImages($('#mainContent')).catch(()=>{});
  }catch(error){$('#mainContent').innerHTML=`<section class="panel"><div class="empty-state"><h3>No se pudo cargar Operaciones</h3><p>${esc(error.message)}</p><button class="btn" id="retryOperations">Reintentar</button></div></section>`;$('#retryOperations').onclick=()=>renderOperationsAdmin(activeTab)}finally{busy=false}
}
