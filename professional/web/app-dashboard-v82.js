import {$,$$,esc,state,api,money} from './app-core.js';
import {bindDynamic} from './app-actions.js';
import {registerRouteRenderer} from './app-router-v14.js';

let metaCache=null;
const option=(items,value,label='Todos')=>`<option value="">${label}</option>${(items||[]).map(item=>`<option value="${esc(item.id)}" ${String(item.id)===String(value)?'selected':''}>${esc(item.name)}</option>`).join('')}`;
const monthLabel=value=>{try{return new Date(`${value}-01T12:00:00`).toLocaleDateString('es-CL',{month:'short',year:'2-digit'})}catch{return value}};

function injectStyles(){
  if($('#nuvastoDashboardV82Styles'))return;
  const style=document.createElement('style');
  style.id='nuvastoDashboardV82Styles';
  style.textContent=`
.v82-dashboard{display:grid;gap:11px;min-width:0}.v82-head{display:flex;align-items:center;justify-content:space-between;gap:10px;padding:1px 2px}.v82-head h2{margin:0;font-size:22px;line-height:1.1;letter-spacing:-.025em}.v82-head .btn{min-height:40px}
.v82-filters{display:grid;grid-template-columns:repeat(6,minmax(110px,1fr));gap:7px;padding:10px;border:1px solid var(--line);border-radius:16px;background:var(--card)}.v82-filters label{display:grid;gap:4px;min-width:0}.v82-filters label>span{font-size:9px;font-weight:750;letter-spacing:.055em;text-transform:uppercase;color:var(--muted)}.v82-filters input,.v82-filters select{min-width:0;min-height:42px;border:1px solid var(--line);border-radius:11px;background:var(--soft);color:var(--text);padding:0 10px;font-size:14px}.v82-filter-actions{grid-column:1/-1;display:flex;align-items:center;justify-content:space-between;gap:8px;flex-wrap:wrap}.v82-periods{display:flex;gap:6px;flex-wrap:wrap}.v82-periods .btn{min-height:36px;padding-inline:10px}.v82-periods .active{background:var(--primary)!important;color:#fff!important}
.v82-content{display:grid;gap:10px}.v82-kpis{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:8px}.v82-kpi{position:relative;min-width:0;padding:13px 14px;border:1px solid var(--line);border-radius:17px;background:var(--card);overflow:hidden}.v82-kpi::before{content:'';position:absolute;left:0;right:0;top:0;height:2px;background:transparent}.v82-kpi.primary::before{background:var(--primary)}.v82-kpi.positive::before{background:var(--success)}.v82-kpi.attention::before{background:var(--warning)}.v82-kpi.danger::before{background:var(--danger)}.v82-kpi span,.v82-kpi small{display:block;color:var(--muted)}.v82-kpi span{font-size:10px;font-weight:650}.v82-kpi strong{display:block;margin:6px 0 4px;font-size:20px;line-height:1.05;letter-spacing:-.02em;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.v82-kpi small{font-size:10px;line-height:1.3}.v82-kpi.danger strong{color:var(--danger)}.v82-kpi.attention strong{color:var(--warning)}
.v82-panels{display:grid;grid-template-columns:minmax(0,1.35fr) minmax(270px,.65fr);gap:9px}.v82-panel{min-width:0;padding:14px;border:1px solid var(--line);border-radius:17px;background:var(--card);overflow:hidden}.v82-panel.full{grid-column:1/-1}.v82-panel-head{display:flex;align-items:flex-start;justify-content:space-between;gap:10px;margin-bottom:12px}.v82-panel h3{margin:0;font-size:14px;letter-spacing:-.012em}.v82-panel p{margin:4px 0 0;color:var(--muted);font-size:10px;line-height:1.35}
.v82-chart{display:block;width:100%;height:190px;overflow:visible}.v82-chart-grid{stroke:var(--line);stroke-width:1}.v82-chart-area{fill:color-mix(in srgb,var(--primary) 10%,transparent)}.v82-chart-line{fill:none;stroke:var(--primary);stroke-width:3;stroke-linecap:round;stroke-linejoin:round}.v82-chart-dot{fill:var(--card);stroke:var(--primary);stroke-width:2}.v82-chart-labels{display:grid;grid-template-columns:repeat(var(--count),1fr);gap:2px;margin-top:3px;color:var(--muted);font-size:9px;text-align:center}
.v82-bars{display:grid;gap:10px}.v82-bar{display:grid;grid-template-columns:minmax(84px,.4fr) minmax(100px,1fr) auto;gap:8px;align-items:center;min-width:0}.v82-bar>span{overflow:hidden;text-overflow:ellipsis;white-space:nowrap;color:var(--muted);font-size:10px}.v82-bar-track{height:8px;border-radius:999px;background:var(--soft);overflow:hidden}.v82-bar-track i{display:block;height:100%;border-radius:inherit;background:var(--primary)}.v82-bar strong{font-size:10px}
.v82-flow{display:grid;gap:10px}.v82-flow-row{display:grid;grid-template-columns:58px minmax(0,1fr) auto;gap:8px;align-items:center}.v82-flow-row>span{font-size:9px;color:var(--muted)}.v82-flow-bars{display:grid;gap:4px}.v82-flow-track{height:7px;border-radius:999px;background:var(--soft);overflow:hidden}.v82-flow-track i{display:block;height:100%;border-radius:inherit}.v82-flow-track.transferred i{background:var(--success)}.v82-flow-track.due i{background:var(--warning)}.v82-flow-row strong{font-size:9px;text-align:right}.v82-flow-legend{display:flex;gap:12px;flex-wrap:wrap;margin-bottom:10px;color:var(--muted);font-size:9px}.v82-flow-legend span{display:inline-flex;align-items:center;gap:5px}.v82-flow-legend i{width:8px;height:8px;border-radius:50%}.v82-flow-legend .transferred{background:var(--success)}.v82-flow-legend .due{background:var(--warning)}
.v82-status{display:grid;grid-template-columns:repeat(4,1fr);gap:7px}.v82-status article{padding:10px;border-radius:13px;background:var(--soft)}.v82-status strong,.v82-status small{display:block}.v82-status strong{font-size:17px}.v82-status small{margin-top:3px;color:var(--muted);font-size:9px}.v82-note,.v82-loading{padding:18px;border-radius:15px;background:var(--soft);color:var(--muted);font-size:11px}.v82-loading{min-height:180px;display:grid;place-items:center}
@media(max-width:960px){.v82-filters{grid-template-columns:repeat(3,1fr)}.v82-kpis{grid-template-columns:repeat(2,1fr)}.v82-panels{grid-template-columns:1fr}.v82-panel.full{grid-column:auto}}
@media(max-width:620px){.v82-dashboard{gap:9px}.v82-head h2{font-size:20px}.v82-filters{grid-template-columns:1fr 1fr;padding:9px}.v82-filter-actions{display:grid;grid-template-columns:1fr}.v82-periods{display:grid;grid-template-columns:repeat(4,1fr)}.v82-kpi{padding:11px 12px;border-radius:15px}.v82-kpi strong{font-size:18px}.v82-panel{padding:12px;border-radius:15px}.v82-status{grid-template-columns:1fr 1fr}.v82-bar{grid-template-columns:78px 1fr}.v82-bar strong{grid-column:2;text-align:right}.v82-flow-row{grid-template-columns:48px 1fr}.v82-flow-row strong{grid-column:2}}
`;
  document.head.append(style);
}

function lineChart(monthly){
  const source=monthly||[],values=source.map(item=>Number(item.spend||item.estimatedSpend||0)),max=Math.max(1,...values),width=760,height=190,pad=22;
  if(!source.length)return'<div class="v82-note">Sin compras facturadas en el período.</div>';
  const points=values.map((value,index)=>({x:values.length===1?width/2:pad+index*(width-pad*2)/(values.length-1),y:height-pad-value/max*(height-pad*2),value})),poly=points.map(point=>`${point.x},${point.y}`).join(' '),area=`${pad},${height-pad} ${poly} ${width-pad},${height-pad}`;
  return`<svg class="v82-chart" viewBox="0 0 ${width} ${height}" preserveAspectRatio="none" role="img" aria-label="Evolución de compras">${[.33,.66,1].map(step=>`<line class="v82-chart-grid" x1="${pad}" x2="${width-pad}" y1="${height-pad-step*(height-pad*2)}" y2="${height-pad-step*(height-pad*2)}"/>`).join('')}<polygon class="v82-chart-area" points="${area}"/><polyline class="v82-chart-line" points="${poly}"/>${points.map(point=>`<circle class="v82-chart-dot" cx="${point.x}" cy="${point.y}" r="4"><title>${money(point.value)}</title></circle>`).join('')}</svg><div class="v82-chart-labels" style="--count:${Math.max(1,source.length)}">${source.map(item=>`<span>${esc(monthLabel(item.month).split(' ')[0])}</span>`).join('')}</div>`;
}
function bars(items,labelKey,valueKey){const source=items||[],max=Math.max(1,...source.map(item=>Number(item[valueKey]||0)));return`<div class="v82-bars">${source.slice(0,7).map(item=>`<div class="v82-bar"><span>${esc(item[labelKey]||'Sin nombre')}</span><span class="v82-bar-track"><i style="width:${Math.max(2,Number(item[valueKey]||0)/max*100)}%"></i></span><strong>${money(item[valueKey]||0)}</strong></div>`).join('')||'<div class="v82-note">Sin datos en el período.</div>'}</div>`}
function statusGrid(items){const totals={draft:0,open:0,received:0,cancelled:0};for(const item of items||[]){if(item.status==='draft')totals.draft+=Number(item.total||0);else if(item.status==='cancelled')totals.cancelled+=Number(item.total||0);else if(['received','reconciled','closed'].includes(item.status))totals.received+=Number(item.total||0);else totals.open+=Number(item.total||0)}return`<div class="v82-status"><article><strong>${totals.draft}</strong><small>En edición</small></article><article><strong>${totals.open}</strong><small>Emitidos</small></article><article><strong>${totals.received}</strong><small>Recibidos/cerrados</small></article><article><strong>${totals.cancelled}</strong><small>Anulados</small></article></div>`}
function cashFlow(finance){
  if(!finance)return'<div class="v82-note">La posición financiera requiere permiso de Finanzas.</div>';
  const months=new Map();for(const item of finance.transferTimeline||[])months.set(item.month,{month:item.month,transferred:Number(item.amount||0),due:0});for(const item of finance.dueTimeline||[]){const row=months.get(item.month)||{month:item.month,transferred:0,due:0};row.due=Number(item.amount||0);months.set(item.month,row)}const rows=[...months.values()].sort((a,b)=>a.month.localeCompare(b.month)),max=Math.max(1,...rows.flatMap(item=>[item.transferred,item.due]));
  return`<div class="v82-flow-legend"><span><i class="transferred"></i>Transferido</span><span><i class="due"></i>Cargos por vencimiento</span></div><div class="v82-flow">${rows.map(item=>`<div class="v82-flow-row"><span>${esc(monthLabel(item.month))}</span><div class="v82-flow-bars"><span class="v82-flow-track transferred"><i style="width:${Math.max(item.transferred?3:0,item.transferred/max*100)}%"></i></span><span class="v82-flow-track due"><i style="width:${Math.max(item.due?3:0,item.due/max*100)}%"></i></span></div><strong>${item.transferred?money(item.transferred):'—'} / ${item.due?money(item.due):'—'}</strong></div>`).join('')||'<div class="v82-note">Sin movimientos financieros para mostrar.</div>'}</div>`;
}
function panel(title,description,body,full=false){return`<article class="v82-panel ${full?'full':''}"><div class="v82-panel-head"><div><h3>${esc(title)}</h3><p>${esc(description)}</p></div></div>${body}</article>`}
function kpi(label,value,detail,className=''){return`<article class="v82-kpi ${className}"><span>${esc(label)}</span><strong>${esc(value)}</strong><small>${esc(detail)}</small></article>`}
function renderSummary(analytics,finance){
  const m=analytics.metrics||{},delta=Number(m.monthDeltaPct||0),f=finance?.metrics||null,cards=[];
  cards.push(kpi('Compra facturada',money(m.spend||0),`${Number(m.invoiceCount||0)} factura${Number(m.invoiceCount||0)===1?'':'s'}`,'primary'));
  if(f){cards.push(kpi('Fondos transferidos',money(f.transferred||0),`${Number(f.transferDocuments||0)} movimiento${Number(f.transferDocuments||0)===1?'':'s'} en el período`,'positive'));cards.push(kpi('Saldo por pagar',money(f.outstanding||0),`${Number(f.outstandingDocuments||0)} documento${Number(f.outstandingDocuments||0)===1?'':'s'} abierto${Number(f.outstandingDocuments||0)===1?'':'s'}`));cards.push(kpi('Comprometido',money(f.committed||0),`${Number(f.openOrders||0)} pedido${Number(f.openOrders||0)===1?'':'s'} abierto${Number(f.openOrders||0)===1?'':'s'}`));cards.push(kpi('Próximos 60 días',money(f.next60Days||0),`${Number(f.next60Documents||0)} vencimiento${Number(f.next60Documents||0)===1?'':'s'}`,'attention'));cards.push(kpi('Vencido',money(f.overdue||0),`${Number(f.overdueDocuments||0)} documento${Number(f.overdueDocuments||0)===1?'':'s'}`,'danger'))}
  cards.push(kpi('Pedidos',String(Number(m.orders||0)),`${Number(m.received||0)} recibidos · ${Number(m.pending||0)} pendientes`));cards.push(kpi('Variación',`${delta>0?'+':''}${delta}%`,'vs. mes anterior',delta>0?'attention':delta<0?'positive':''));
  return`<section class="v82-kpis">${cards.join('')}</section>`;
}
function renderData(analytics,finance){const target=$('#v82DashboardContent');if(!target)return;target.innerHTML=`${renderSummary(analytics,finance)}<section class="v82-panels">${panel('Evolución de compras','Compra facturada por mes.',lineChart(analytics.monthly),true)}${panel('Flujo financiero','Transferencias realizadas frente a cargos con vencimiento.',cashFlow(finance),true)}${panel('Estado operacional','Pedidos según etapa del flujo.',statusGrid(analytics.statusBreakdown))}${panel('Principales proveedores','Participación del gasto facturado.',bars(analytics.topSuppliers,'name','spend'))}${panel('Gasto por categoría','Compra facturada por categoría.',bars(analytics.categorySpend,'category','spend'))}${panel('Gasto por centro','Compra facturada por centro de costo.',bars(analytics.costCenterSpend,'name','spend'))}</section>`}
function params(){const form=$('#v82DashboardFilters'),query=new URLSearchParams();if(!form)return query;new FormData(form).forEach((value,key)=>{if(value)query.set(key,String(value))});return query}
async function load(){const target=$('#v82DashboardContent');if(!target)return;target.innerHTML='<div class="v82-loading">Actualizando resumen…</div>';const query=params();try{const [analyticsResult,financeResult]=await Promise.allSettled([api(`/api/dashboard/analytics-v40?${query}`,{fresh:true,timeout:30000}),api(`/api/dashboard/finance-v81?${query}`,{fresh:true,timeout:30000})]);if(analyticsResult.status!=='fulfilled')throw analyticsResult.reason;renderData(analyticsResult.value.analytics,financeResult.status==='fulfilled'?financeResult.value.finance:null)}catch(error){target.innerHTML=`<div class="v82-note" role="alert">${esc(error.message||'No se pudo cargar el resumen')}</div>`}}
function setPeriod(days){const form=$('#v82DashboardFilters');if(!form)return;const to=new Date(),from=new Date();from.setDate(to.getDate()-days+1);form.elements.from.value=from.toISOString().slice(0,10);form.elements.to.value=to.toISOString().slice(0,10);$$('[data-v82-period]').forEach(button=>button.classList.toggle('active',Number(button.dataset.v82Period)===days));load()}
async function meta(){if(metaCache)return metaCache;const [locations,costCenters,categories,suppliers]=await Promise.all([api('/api/locations'),api('/api/cost-centers'),api('/api/categories'),api('/api/suppliers')]);metaCache={locations:locations.locations||[],costCenters:costCenters.costCenters||[],categories:categories.categories||[],suppliers:suppliers.suppliers||[]};return metaCache}

export async function renderDashboardV82(){
  injectStyles();state.view='dashboard';state.subview='';$$('.nav-item[data-view],.bottom-item[data-view]').forEach(button=>button.classList.toggle('active',button.dataset.view==='dashboard'));if($('#pageEyebrow'))$('#pageEyebrow').textContent='';if($('#pageTitle'))$('#pageTitle').textContent='Resumen';if($('#primaryAction')){$('#primaryAction').classList.remove('hidden');$('#primaryAction').innerHTML='<span>＋</span><span>Nuevo pedido</span>'}
  const data=await meta();$('#mainContent').innerHTML=`<section class="v82-dashboard"><header class="v82-head"><h2>Resumen</h2><button class="btn primary" data-action="new-order">＋ Pedido</button></header><form class="v82-filters" id="v82DashboardFilters"><label><span>Desde</span><input type="date" name="from"></label><label><span>Hasta</span><input type="date" name="to"></label><label><span>Local</span><select name="locationId">${option(data.locations,'','Todos')}</select></label><label><span>Centro</span><select name="costCenterId">${option(data.costCenters,'','Todos')}</select></label><label><span>Categoría</span><select name="categoryId">${option(data.categories,'','Todas')}</select></label><label><span>Proveedor</span><select name="supplierId">${option(data.suppliers,'','Todos')}</select></label><div class="v82-filter-actions"><div class="v82-periods"><button class="btn small" type="button" data-v82-period="30">30d</button><button class="btn small" type="button" data-v82-period="90">3m</button><button class="btn small active" type="button" data-v82-period="183">6m</button><button class="btn small" type="button" data-v82-period="365">12m</button></div><div><button class="btn" type="button" id="v82Clear">Limpiar</button><button class="btn primary" type="submit">Aplicar</button></div></div></form><div class="v82-content" id="v82DashboardContent"></div></section>`;
  $('#v82DashboardFilters').onsubmit=event=>{event.preventDefault();load()};$$('[data-v82-period]').forEach(button=>button.onclick=()=>setPeriod(Number(button.dataset.v82Period)));$('#v82Clear').onclick=()=>{$('#v82DashboardFilters').reset();setPeriod(183)};bindDynamic();setPeriod(183)
}
export function initializeDashboardV82(){injectStyles();registerRouteRenderer('dashboard',renderDashboardV82)}
