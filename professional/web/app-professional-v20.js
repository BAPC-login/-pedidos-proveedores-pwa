import {$,$$,esc,money,state,api,toast,setBusy} from './app-core.js';
import {openModal,closeModal} from './app-modal.js';
import {openRoute,registerRouteRenderer} from './app-router-v14.js';

let initialized=false,overview=null;
const bytes=value=>{const amount=Number(value||0);if(amount<1024)return`${amount} B`;if(amount<1024**2)return`${(amount/1024).toFixed(1)} KB`;if(amount<1024**3)return`${(amount/1024**2).toFixed(1)} MB`;return`${(amount/1024**3).toFixed(2)} GB`};
const badge=(ok,label)=>`<span class="v20-badge ${ok?'ok':'pending'}">${ok?'✓':'!'} ${esc(label)}</span>`;
function injectStyles(){if($('#professionalV20Styles'))return;const style=document.createElement('style');style.id='professionalV20Styles';style.textContent=`
.professional-v20{display:grid;gap:14px;max-width:1120px;margin:0 auto}.v20-hero{display:grid;grid-template-columns:minmax(0,1fr) auto;gap:16px;align-items:center;padding:20px;border:1px solid var(--line);border-radius:20px;background:linear-gradient(135deg,color-mix(in srgb,var(--primary) 10%,var(--card)),var(--card))}.v20-hero h2{margin:5px 0;font-size:30px}.v20-hero p{max-width:760px;margin:0;color:var(--muted);font-size:10px;line-height:1.55}.v20-score{width:88px;height:88px;display:grid;place-items:center;border-radius:50%;background:conic-gradient(var(--success) calc(var(--score)*1%),var(--soft) 0);position:relative}.v20-score:after{content:'';position:absolute;inset:8px;border-radius:50%;background:var(--card)}.v20-score strong{position:relative;z-index:1;font-size:22px}.v20-grid{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:10px}.v20-card{display:grid;gap:10px;padding:15px;border:1px solid var(--line);border-radius:16px;background:var(--card)}.v20-card header{display:flex;align-items:flex-start;justify-content:space-between;gap:8px}.v20-card h3{margin:0;font-size:14px}.v20-card p{margin:4px 0 0;color:var(--muted);font-size:8px;line-height:1.45}.v20-card strong.metric{font-size:24px}.v20-card-actions{display:flex;flex-wrap:wrap;gap:6px;margin-top:auto}.v20-badge{display:inline-flex;align-items:center;min-height:23px;padding:0 8px;border-radius:999px;font-size:7px;font-weight:900}.v20-badge.ok{color:var(--success);background:color-mix(in srgb,var(--success) 12%,var(--card))}.v20-badge.pending{color:var(--warning);background:color-mix(in srgb,var(--warning) 12%,var(--card))}.v20-list{display:grid;gap:7px}.v20-row{display:grid;grid-template-columns:minmax(0,1fr) auto;gap:9px;align-items:center;padding:10px;border:1px solid var(--line);border-radius:11px;background:var(--soft)}.v20-row strong,.v20-row small{display:block}.v20-row small{margin-top:4px;color:var(--muted);font-size:8px}.v20-recommendations{padding:15px;border:1px solid var(--line);border-radius:16px;background:var(--card)}.v20-recommendations h3{margin:0 0 9px}.v20-recommendations ol{margin:0;padding-left:18px;color:var(--muted);font-size:9px;line-height:1.7}.v20-step{padding:10px 12px;border:1px solid var(--line);border-radius:10px;background:var(--soft);font-size:9px;line-height:1.5}.v20-code{display:block;padding:10px;border-radius:9px;background:#0c1220;color:#e8edf7;font:11px ui-monospace,SFMono-Regular,Menlo,monospace;white-space:pre-wrap;overflow:auto}.v20-brand-choice{display:grid;grid-template-columns:auto minmax(0,1fr);gap:10px;align-items:center;padding:12px;border:1px solid var(--line);border-radius:12px;background:var(--soft)}.v20-brand-choice input{width:22px;height:22px}.v20-brand-choice strong,.v20-brand-choice small{display:block}.v20-brand-choice small{margin-top:4px;color:var(--muted)}
@media(max-width:900px){.v20-grid{grid-template-columns:repeat(2,minmax(0,1fr))}}@media(max-width:560px){.v20-grid{grid-template-columns:1fr}.v20-hero{grid-template-columns:1fr}.v20-score{width:74px;height:74px}.v20-card-actions{display:grid;grid-template-columns:1fr 1fr}.v20-card-actions .btn{width:100%}}
`;document.head.append(style)}
function setActive(){state.view='professional';$$('.nav-item,.bottom-item').forEach(button=>button.classList.remove('active'));if($('#pageEyebrow'))$('#pageEyebrow').textContent='CONTROL PROFESIONAL';if($('#pageTitle'))$('#pageTitle').textContent='Preparación comercial';$('#primaryAction')?.classList.add('hidden')}
function card(title,description,body,actions=''){return`<article class="v20-card"><header><div><h3>${esc(title)}</h3><p>${esc(description)}</p></div></header>${body}<div class="v20-card-actions">${actions}</div></article>`}
async function loadOverview(){overview=await api('/api/professional',{fresh:true});return overview}
function paint(data){const storage=data.storage,security=data.security,approvals=data.approvals,reconciliation=data.reconciliation,onboarding=data.onboarding,connectors=data.connectors,brand=data.brand;$('#mainContent').innerHTML=`<div class="professional-v20"><section class="v20-hero"><div><span class="eyebrow">SUITE V20</span><h2>Seguridad, control y preparación comercial</h2><p>Valida almacenamiento, identidad, aprobaciones, integraciones, conciliación, alertas, onboarding y marca antes de incorporar clientes externos.</p></div><div class="v20-score" style="--score:${data.score}"><strong>${data.score}%</strong></div></section><section class="v20-grid">${card('Almacenamiento de archivos','Facturas, imágenes, logos y PDF.',`${badge(storage.r2Ready,storage.r2Ready?'R2 operativo':'D1 temporal')}<strong class="metric">${bytes(storage.bytes)}</strong><small>${storage.files} archivos almacenados</small>`,`<button class="btn primary" data-v20="storage-probe">Probar almacenamiento</button><button class="btn" data-v20="storage-guide">Configurar R2</button>`)}${card('Google y Cloudflare Access','SSO, identidad y control de acceso.',`${badge(security.accessConfigured,security.accessConfigured?'SSO configurado':'SSO pendiente')}<strong class="metric">${security.sessionHours} h</strong><small>Duración objetivo de sesión</small>`,`<button class="btn primary" data-v20="security">Configurar SSO</button>`)}${card('Aprobación de compras','Límites y revisión antes de emitir.',`${badge(approvals.policy.enabled,approvals.policy.enabled?'Política activa':'Sin política')}<strong class="metric">${approvals.pending}</strong><small>solicitudes pendientes</small>`,`<button class="btn primary" data-v20="approval-policy">Definir política</button><button class="btn" data-v20="approvals">Revisar</button>`)}${card('Conectores de proveedores','Mi Carro, Mi Embonor y otros portales.',`${badge(connectors.some(item=>item.status==='ready'),connectors.some(item=>item.status==='ready')?'Conector listo':'Modo asistido pendiente')}<strong class="metric">${connectors.length}</strong><small>conectores configurados</small>`,`<button class="btn primary" data-v20="connectors">Administrar</button>`)}${card('Conciliación financiera','Pedido, recepción, factura y diferencias.',`${badge(!reconciliation.open.length,!reconciliation.open.length?'Sin diferencias':`${reconciliation.open.length} por revisar`)}<strong class="metric">${reconciliation.open.length}</strong><small>casos abiertos</small>`,`<button class="btn primary" data-v20="reconciliation">Abrir cola</button>`)}${card('Alertas operativas','Factura faltante, atraso y precio.',`${badge(data.alerts.some(item=>item.enabled),'Reglas disponibles')}<strong class="metric">${data.alerts.filter(item=>item.enabled).length}</strong><small>reglas activas</small>`,`<button class="btn primary" data-v20="alerts">Configurar</button>`)}${card('Pruebas y recuperación','QA real, respaldos y restauración.',`${badge(onboarding.lastQa?.status==='passed',onboarding.lastQa?.status==='passed'?'QA aprobado':'QA pendiente')}<strong class="metric">${onboarding.backupCount}</strong><small>respaldos disponibles</small>`,`<button class="btn" data-v20="readiness">Abrir preparación comercial</button>`)}${card('Marca del producto','Nombre, promesa y futura identidad visual.',`${badge(brand.status!=='exploring',brand.status==='registered'?'Marca registrada':brand.status==='selected'?'Nombre seleccionado':'Nombre pendiente')}<strong class="metric">${esc(brand.productName)}</strong><small>${esc(brand.tagline)}</small>`,`<button class="btn primary" data-v20="brand">Definir marca</button>`)}${card('Onboarding de clientes','Empresa, locales, usuarios y catálogo.',`${badge(onboarding.currentStep>=6,onboarding.currentStep>=6?'Configuración completa':'En progreso')}<strong class="metric">${onboarding.currentStep}</strong><small>etapa actual del onboarding</small>`,`<button class="btn" data-v20="readiness">Continuar onboarding</button>`)}</section><section class="v20-recommendations"><h3>Próximas acciones</h3>${data.recommendations.length?`<ol>${data.recommendations.map(item=>`<li>${esc(item)}</li>`).join('')}</ol>`:'<p>La configuración profesional está completa.</p>'}</section></div>`;bindActions()}
async function refresh(){setActive();$('#mainContent').innerHTML='<section class="panel"><div class="empty-state compact-empty">Revisando configuración profesional…</div></section>';try{paint(await loadOverview())}catch(error){$('#mainContent').innerHTML=`<section class="panel"><div class="empty-state"><h3>No se pudo abrir el centro profesional</h3><p>${esc(error.message)}</p></div></section>`}}
async function storageProbe(){const button=$('[data-v20="storage-probe"]');setBusy(button,true,'Probando…');try{const result=await api('/api/professional/storage/probe',{method:'POST',json:{}});toast(result.run.status==='passed'?'R2 escribió, leyó y eliminó correctamente':result.run.details.message,result.run.status==='failed'?'error':'ok');await refresh()}catch(error){toast(error.message,'error')}finally{setBusy(button,false)}}
function storageGuide(){openModal({eyebrow:'ALMACENAMIENTO R2',title:'Activar R2 sin pagar mientras estés bajo el tramo gratuito',subtitle:'Cloudflare exige habilitar la suscripción y un método de pago, aunque el uso incluido puede resultar en $0.',size:'large',hideSubmit:true,body:`<div class="v20-list"><div class="v20-step"><strong>1. Habilitar la suscripción</strong><br>Cloudflare → Storage & databases → R2 → Overview → completar checkout.</div><div class="v20-step"><strong>2. Crear el bucket privado</strong><br>Nombre exacto: <b>pedidos-pro-files</b>. Clase: Standard.</div><div class="v20-step"><strong>3. Cambiar el despliegue</strong><br>Usar el perfil R2 incluido en el repositorio.</div><code class="v20-code">npx wrangler r2 bucket create pedidos-pro-files\nnpx wrangler deploy --config wrangler.r2.toml</code><div class="v20-step"><strong>4. Verificar</strong><br>Volver a este centro y presionar “Probar almacenamiento”. Debe validar escritura, lectura y eliminación.</div></div>`})}
async function securityModal(){const settings=(await api('/api/professional/security')).settings;openModal({eyebrow:'IDENTIDAD Y SSO',title:'Google mediante Cloudflare Access',subtitle:'Access protege la aplicación antes de que llegue al Worker; el Worker valida además la firma JWT.',size:'large',body:`<div class="form-grid"><label class="check-card full"><input name="accessEnabled" type="checkbox" ${settings.accessEnabled?'checked':''}><span><strong>Activar SSO como método adicional</strong><small>La contraseña interna continúa disponible como recuperación.</small></span></label><label class="field"><span>Team domain</span><input name="teamDomain" value="${esc(settings.teamDomain||'')}"></label><label class="field"><span>Application AUD</span><input name="audience" value="${esc(settings.audience||'')}"></label><label class="field"><span>Dominios permitidos</span><input name="allowedDomains" value="${esc((settings.allowedDomains||[]).join(', '))}"></label><label class="field"><span>Duración de sesión (horas)</span><input name="sessionHours" type="number" min="1" max="168" value="${settings.sessionHours}"></label><label class="check-card"><input name="requireMfa" type="checkbox" ${settings.requireMfa?'checked':''}><span><strong>Exigir MFA</strong><small>Se aplica en la política de Access.</small></span></label></div><div class="v20-list" style="margin-top:12px"><div class="v20-step">Zero Trust → Integrations → Identity providers → Google.</div><div class="v20-step">Access controls → Applications → Self-hosted → proteger el dominio de la PWA.</div><div class="v20-step">Crear una política Allow para los correos o dominio autorizado.</div><code class="v20-code">CF_ACCESS_TEAM_DOMAIN = "${esc(settings.teamDomain||'tu-equipo.cloudflareaccess.com')}"\nCF_ACCESS_AUD = "${esc(settings.audience||'audience-tag')}"</code></div>`,submitLabel:'Guardar configuración',onSubmit:async form=>{await api('/api/professional/security',{method:'PUT',json:{accessEnabled:form.get('accessEnabled')==='on',teamDomain:form.get('teamDomain'),audience:form.get('audience'),allowedDomains:String(form.get('allowedDomains')||'').split(',').map(value=>value.trim()).filter(Boolean),sessionHours:Number(form.get('sessionHours')||12),requireMfa:form.get('requireMfa')==='on'}});toast('Configuración SSO guardada. Falta aplicar las variables en Cloudflare.');await refresh()}})}
async function approvalPolicy(){const policy=(await api('/api/professional/approval-policy')).policy;openModal({eyebrow:'APROBACIONES',title:'Política antes de emitir',subtitle:'Los compradores reciben una solicitud pendiente cuando el archivo supera las reglas.',body:`<div class="form-grid"><label class="check-card full"><input name="enabled" type="checkbox" ${policy.enabled?'checked':''}><span><strong>Activar aprobación previa</strong><small>Owner, administrador y aprobador pueden resolver solicitudes.</small></span></label><label class="field"><span>Monto desde el cual exigir aprobación</span><input name="amountThreshold" type="number" min="0" value="${policy.amountThreshold}"></label><label class="field"><span>Rol aprobador</span><select name="approverRole"><option value="approver" ${policy.approverRole==='approver'?'selected':''}>Aprobador</option><option value="admin" ${policy.approverRole==='admin'?'selected':''}>Administrador</option><option value="owner" ${policy.approverRole==='owner'?'selected':''}>Owner</option></select></label><label class="check-card full"><input name="requireUnknownPrice" type="checkbox" ${policy.requireUnknownPrice?'checked':''}><span><strong>Revisar productos sin precio conocido</strong><small>Evita emitir compras cuyo monto todavía no puede estimarse.</small></span></label></div>`,submitLabel:'Guardar política',onSubmit:async form=>{await api('/api/professional/approval-policy',{method:'PUT',json:{enabled:form.get('enabled')==='on',amountThreshold:Number(form.get('amountThreshold')||0),approverRole:form.get('approverRole'),requireUnknownPrice:form.get('requireUnknownPrice')==='on'}});toast('Política de aprobación actualizada');await refresh()}})}
async function approvalsModal(){const requests=(await api('/api/professional/approvals')).requests;openModal({eyebrow:'APROBACIONES',title:'Solicitudes de compra',subtitle:'Revisa montos, precios desconocidos y folios antes de permitir la emisión.',size:'large',hideSubmit:true,body:`<div class="v20-list">${requests.map(item=>`<article class="v20-row" data-approval-row="${esc(item.id)}"><div><strong>${esc((item.trigger.folios||[]).join(', ')||item.batchId)}</strong><small>${money(item.trigger.total||0)} · ${Number(item.trigger.unknownPrices||0)} precios desconocidos · ${esc(item.requestedBy)}</small></div>${item.status==='pending'?`<div class="v20-card-actions"><button class="btn small primary" data-approval="approved" data-id="${esc(item.id)}">Aprobar</button><button class="btn small danger" data-approval="rejected" data-id="${esc(item.id)}">Rechazar</button></div>`:badge(item.status==='approved',item.status)}</article>`).join('')||'<div class="empty-state compact-empty">No hay solicitudes.</div>'}</div>`});$$('[data-approval]').forEach(button=>button.onclick=async()=>{const decision=button.dataset.approval;if(!confirm(`${decision==='approved'?'Aprobar':'Rechazar'} esta solicitud?`))return;await api(`/api/professional/approvals/${button.dataset.id}/decision`,{method:'POST',json:{decision}});button.closest('[data-approval-row]')?.remove();toast(decision==='approved'?'Compra aprobada':'Compra rechazada')})}
async function connectorCenter(){const[suppliersPayload,connectorsPayload,ordersPayload]=await Promise.all([api('/api/suppliers'),api('/api/professional/connectors'),api('/api/orders')]),suppliers=suppliersPayload.suppliers||[],connectors=connectorsPayload.connectors||[],orders=(ordersPayload.orders||[]).filter(order=>order.publicState==='emitted');openModal({eyebrow:'CONECTORES',title:'Portales de proveedores',subtitle:'El modo asistido prepara el carrito gratis. API y RPA permanecen bloqueados hasta autorización y credenciales oficiales.',size:'large',hideSubmit:true,body:`<div class="v20-card-actions"><button class="btn primary" id="newConnectorV20">＋ Agregar conector</button></div><div class="v20-list" style="margin-top:12px">${connectors.map(item=>`<article class="v20-row"><div><strong>${esc(item.supplierName)} · ${esc(item.displayName)}</strong><small>${esc(item.providerCode)} · ${esc(item.mode)} · ${esc(item.status)}${item.lastError?` · ${esc(item.lastError)}`:''}</small></div><div class="v20-card-actions"><button class="btn small" data-test-connector="${esc(item.id)}">Probar</button><button class="btn small primary" data-prepare-connector="${esc(item.id)}">Preparar pedido</button></div></article>`).join('')||'<div class="empty-state compact-empty">Aún no hay conectores.</div>'}</div>`});$('#newConnectorV20').onclick=()=>{openModal({eyebrow:'NUEVO CONECTOR',title:'Configurar proveedor',body:`<div class="form-grid"><label class="field"><span>Proveedor</span><select name="supplierId">${suppliers.map(item=>`<option value="${esc(item.id)}">${esc(item.name)}</option>`).join('')}</select></label><label class="field"><span>Plataforma</span><select name="providerCode"><option value="mi-carro">Mi Carro · CCU / Pisquera</option><option value="mi-embonor">Mi Embonor</option><option value="generic">Otro proveedor</option></select></label><label class="field"><span>Modo</span><select name="mode"><option value="assisted">Asistido: preparar carrito</option><option value="email">Correo estructurado</option><option value="api">API oficial</option><option value="rpa">RPA supervisado</option></select></label><label class="field"><span>Nombre visible</span><input name="displayName" value="Portal de pedidos"></label><label class="field full"><span>Correo de pedidos, cuando aplique</span><input name="orderEmail" type="email"></label><label class="field full"><span>Endpoint oficial, cuando exista</span><input name="baseUrl" type="url"></label><label class="field full"><span>Referencia del secreto en Cloudflare</span><input name="secretRef"></label></div>`,submitLabel:'Guardar conector',onSubmit:async form=>{await api('/api/professional/connectors',{method:'POST',json:{supplierId:form.get('supplierId'),providerCode:form.get('providerCode'),mode:form.get('mode'),displayName:form.get('displayName'),secretRef:form.get('secretRef'),config:{orderEmail:form.get('orderEmail'),baseUrl:form.get('baseUrl')}}});toast('Conector guardado');setTimeout(connectorCenter,0)}})};$$('[data-test-connector]').forEach(button=>button.onclick=async()=>{setBusy(button,true,'Probando…');try{const result=await api(`/api/professional/connectors/${button.dataset.testConnector}/test`,{method:'POST',json:{}});toast(result.result.message,result.result.status==='ready'?'ok':'error')}finally{setBusy(button,false)}});$$('[data-prepare-connector]').forEach(button=>button.onclick=()=>{const connector=connectors.find(item=>item.id===button.dataset.prepareConnector),eligible=orders.filter(order=>order.supplierId===connector?.supplierId);openModal({eyebrow:'ASISTENTE DE PORTAL',title:connector?.supplierName||'Proveedor',subtitle:'Selecciona un pedido emitido para preparar el carrito. La compra no se confirma automáticamente.',hideSubmit:true,body:`<div class="v20-list">${eligible.map(order=>`<button class="v20-row" type="button" data-prepare-order="${esc(order.id)}" data-connector="${esc(connector.id)}"><div><strong>${esc(order.folio)}</strong><small>${esc(order.costCenterName)} · ${order.itemCount} productos · ${esc(order.deliveryDate||'sin fecha')}</small></div><b>→</b></button>`).join('')||'<div class="empty-state compact-empty">No hay pedidos emitidos para este proveedor.</div>'}</div>`});$$('[data-prepare-order]').forEach(orderButton=>orderButton.onclick=async()=>{const payload=await api(`/api/professional/connectors/${orderButton.dataset.connector}/orders/${orderButton.dataset.prepareOrder}/prepare`,{method:'POST',json:{}}),attempt=payload.attempt;openModal({eyebrow:'CARRITO PREPARADO',title:attempt.payload.folio,subtitle:'Copia la lista al portal y confirma manualmente precio, disponibilidad, pago y despacho.',hideSubmit:true,body:`<code class="v20-code" id="preparedOrderText">${esc(attempt.plainText)}</code><div class="v20-card-actions" style="margin-top:10px"><button class="btn primary" id="copyPreparedOrder">Copiar lista</button></div><div class="v20-step" style="margin-top:10px">${esc(attempt.reason)}</div>`});$('#copyPreparedOrder').onclick=async()=>{await navigator.clipboard.writeText(attempt.plainText);toast('Lista copiada')}})})})}
async function reconciliationModal(){const data=await api('/api/professional/reconciliation');openModal({eyebrow:'CONCILIACIÓN',title:'Casos que requieren revisión',subtitle:'Pedido versus factura y recepción.',size:'large',hideSubmit:true,body:`<div class="v20-list">${data.open.map(item=>`<article class="v20-row"><div><strong>${esc(item.folio)} · ${esc(item.supplierName)}</strong><small>${item.issues.map(esc).join(' · ')} · esperado ${item.expectedTotal?money(item.expectedTotal):'sin precio'} · factura ${item.invoicedTotal?money(item.invoicedTotal):'pendiente'}</small></div><button class="btn small" data-open-history-order="${esc(item.id)}">Historial</button></article>`).join('')||'<div class="empty-state compact-empty">No hay diferencias abiertas.</div>'}</div>`});$$('[data-open-history-order]').forEach(button=>button.onclick=()=>{closeModal('history');openRoute('history','',{replace:false});setTimeout(()=>document.querySelector(`[data-v18-detail="${CSS.escape(button.dataset.openHistoryOrder)}"]`)?.click(),180)})}
async function alertsModal(){const rules=(await api('/api/professional/alerts')).rules;openModal({eyebrow:'ALERTAS OPERATIVAS',title:'Reglas de seguimiento',subtitle:'Activa el canal y define el umbral de cada evento.',size:'large',hideSubmit:true,body:`<div class="v20-list">${rules.map(item=>`<article class="v20-row" data-alert-row="${esc(item.id)}"><div><strong>${esc(item.ruleKey.replaceAll('_',' '))}</strong><small>${esc(item.channel)} · ${esc(JSON.stringify(item.threshold))}</small></div><label class="check-card"><input type="checkbox" ${item.enabled?'checked':''} data-alert-toggle="${esc(item.id)}"><span><strong>${item.enabled?'Activa':'Inactiva'}</strong></span></label></article>`).join('')}</div>`});$$('[data-alert-toggle]').forEach(input=>input.onchange=async()=>{const current=rules.find(item=>item.id===input.dataset.alertToggle);await api(`/api/professional/alerts/${input.dataset.alertToggle}`,{method:'PATCH',json:{enabled:input.checked,channel:current.channel,threshold:current.threshold,recipients:current.recipients}});toast('Regla actualizada')})}
async function brandModal(){const brand=(await api('/api/professional/brand')).brand;openModal({eyebrow:'TALLER DE MARCA',title:'Definir nombre antes del logo',subtitle:'La selección guarda una decisión interna. El registro definitivo requiere revisar semejanzas y clases en INAPI.',size:'large',body:`<div class="v20-list">${brand.candidates.map((candidate,index)=>`<label class="v20-brand-choice"><input type="radio" name="productName" value="${esc(candidate.name)}" ${candidate.name===brand.productName||(!index&&brand.productName==='Pedidos Pro')?'checked':''}><span><strong>${esc(candidate.name)}</strong><small>${esc(candidate.tagline)}</small></span></label>`).join('')}</div><div class="form-grid" style="margin-top:12px"><label class="field full"><span>Nombre alternativo</span><input name="customName"></label><label class="field full"><span>Promesa de marca</span><input name="tagline" value="${esc(brand.tagline)}"></label><label class="field"><span>Estado</span><select name="status"><option value="exploring" ${brand.status==='exploring'?'selected':''}>Explorando</option><option value="selected" ${brand.status==='selected'?'selected':''}>Seleccionado</option><option value="registered" ${brand.status==='registered'?'selected':''}>Registrado</option></select></label></div>`,submitLabel:'Guardar decisión de marca',onSubmit:async form=>{const custom=String(form.get('customName')||'').trim(),selected=custom||String(form.get('productName')||'Pedidos Pro');await api('/api/professional/brand',{method:'PUT',json:{productName:selected,tagline:form.get('tagline'),status:form.get('status'),candidates:brand.candidates,palette:brand.palette}});toast(`Marca de trabajo guardada: ${selected}`);await refresh()}})}
function bindActions(){$$('[data-v20]').forEach(button=>button.onclick=()=>{const action=button.dataset.v20;if(action==='storage-probe')return storageProbe();if(action==='storage-guide')return storageGuide();if(action==='security')return securityModal();if(action==='approval-policy')return approvalPolicy();if(action==='approvals')return approvalsModal();if(action==='connectors')return connectorCenter();if(action==='reconciliation')return reconciliationModal();if(action==='alerts')return alertsModal();if(action==='brand')return brandModal();if(action==='readiness')return openRoute('enterprise','')})}
function injectEnterpriseCard(){const grid=$('.enterprise-grid');if(!grid||grid.querySelector('[data-professional-v20]'))return;grid.insertAdjacentHTML('afterbegin',`<button class="enterprise-card" type="button" data-professional-v20><span class="enterprise-icon">◆</span><div><strong>Control profesional v20</strong><p>R2, Google SSO, aprobaciones, conectores, conciliación, alertas y marca.</p><small>Preparación comercial</small></div><b>→</b></button>`);grid.querySelector('[data-professional-v20]').onclick=()=>openRoute('professional','')}
export function initializeProfessionalV20(){if(initialized)return;initialized=true;injectStyles();registerRouteRenderer('professional',refresh);window.addEventListener('pedidos:view-rendered',event=>{if(event.detail?.view==='enterprise')injectEnterpriseCard()});requestAnimationFrame(injectEnterpriseCard)}
