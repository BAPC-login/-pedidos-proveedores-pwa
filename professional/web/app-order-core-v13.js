import {$,$$,esc,state,api,toast} from './app-core.js';
import {openModal,closeModal} from './app-modal.js';
import {openOrderDetail} from './app-order-detail.js';
import {loadProcurementSettings,configForCenter,groupProductsByConfiguredOrder,unitsForCenter} from './app-procurement-settings.js';

let initialized=false,settingsCache=null;
const navigate=async view=>(await import('./app-views.js')).navigate(view);
const today=()=>new Date().toISOString().slice(0,10);
const tomorrow=()=>{const date=new Date();date.setDate(date.getDate()+1);return date.toISOString().slice(0,10)};

function injectStyles(){
  if($('#orderCoreV13Styles'))return;
  const style=document.createElement('style');style.id='orderCoreV13Styles';style.textContent=`
    .order-file-builder{display:grid;gap:9px}.order-file-context{display:grid;grid-template-columns:minmax(150px,.75fr) minmax(150px,.65fr) minmax(190px,.8fr);gap:8px;align-items:end}.order-file-local{display:grid;gap:3px;padding:8px 10px;border:1px solid var(--line);border-radius:10px;background:var(--soft)}.order-file-local span{color:var(--muted);font-size:7px;font-weight:850;text-transform:uppercase}.order-file-local strong{font-size:11px}.date-picker-box{display:grid;gap:5px}.date-input-row{display:grid;grid-template-columns:minmax(0,1fr) auto auto;gap:5px}.date-input-row input{min-width:0;min-height:42px;font-size:16px}.date-shortcut{min-width:42px;padding:0 8px}.date-mode{grid-column:1/-1;display:flex;align-items:center;justify-content:space-between;gap:9px;padding:8px 10px;border:1px solid var(--line);border-radius:10px;background:var(--soft)}.date-mode strong{display:block;font-size:9px}.date-mode small{display:block;margin-top:2px;color:var(--muted);font-size:7px}.date-mode input{width:20px;height:20px;accent-color:var(--primary)}.supplier-date-list{grid-column:1/-1;display:grid;grid-template-columns:repeat(auto-fit,minmax(210px,1fr));gap:6px}.supplier-date-row{display:grid;grid-template-columns:minmax(0,1fr) 138px;align-items:center;gap:7px;padding:7px 8px;border:1px solid var(--line);border-radius:9px;background:var(--card)}.supplier-date-row strong{font-size:9px}.supplier-date-row input{min-height:38px;font-size:14px}.order-file-toolbar{display:grid;grid-template-columns:minmax(220px,1fr) auto;gap:8px;align-items:end;padding:8px 10px;border:1px solid var(--line);border-radius:11px;background:var(--soft)}.order-file-summary{display:flex;align-items:baseline;justify-content:flex-end;gap:5px}.order-file-summary strong{font-size:20px}.order-file-summary span{color:var(--muted);font-size:8px}.order-file-list{display:grid;gap:9px}.order-file-warehouse{display:grid;gap:5px}.order-file-warehouse-title{padding:6px 9px;border-left:3px solid var(--primary);background:color-mix(in srgb,var(--primary) 7%,var(--card));font-size:9px;font-weight:900;text-transform:uppercase}.order-file-category{border:1px solid var(--line);border-radius:10px;overflow:hidden;background:var(--card)}.order-file-category-title{padding:6px 9px;border-bottom:1px solid var(--line);background:var(--soft);font-size:8px;font-weight:900;text-transform:uppercase}.order-file-row{display:grid;grid-template-columns:minmax(210px,1fr) 112px 118px 118px;grid-template-areas:'product supplier unit quantity';gap:6px;align-items:center;min-height:48px;padding:5px 7px;border-bottom:1px solid var(--line)}.order-file-row:last-child{border-bottom:0}.order-file-row.selected{background:color-mix(in srgb,var(--primary) 5%,var(--card))}.order-file-product{grid-area:product;min-width:0}.order-file-product strong,.order-file-product small{display:block}.order-file-product strong{font-size:11px;line-height:1.2}.order-file-product small{margin-top:2px;color:var(--muted);font-size:7px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.order-file-control{display:grid;gap:2px;min-width:0}.order-file-control>span{color:var(--muted);font-size:6px;font-weight:850;text-transform:uppercase}.order-file-control select,.order-file-control input,.order-file-supplier-static{width:100%;min-width:0;min-height:35px;border:1px solid var(--line);border-radius:8px;background:var(--card);padding:0 6px}.order-file-supplier{grid-area:supplier}.order-file-unit{grid-area:unit}.order-file-quantity{grid-area:quantity}.order-file-supplier-static{display:flex;align-items:center;color:var(--muted);font-size:8px;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}.quantity-enter-wrap{display:grid;grid-template-columns:minmax(0,1fr) 38px;gap:4px}.quantity-enter-wrap input{font-size:18px;font-weight:900;text-align:center;border-color:color-mix(in srgb,var(--primary) 45%,var(--line))}.quantity-enter-button{min-height:35px!important;padding:0!important;font-size:15px!important;font-weight:900}.order-file-empty{padding:24px 10px;text-align:center;color:var(--muted);border:1px dashed var(--line);border-radius:10px}.batch-file-list{display:grid;gap:7px}.batch-file-card{display:grid;grid-template-columns:minmax(0,1fr) auto;gap:8px;align-items:center;padding:10px;border:1px solid var(--line);border-radius:11px}.batch-file-card strong,.batch-file-card small{display:block}.batch-file-card small{margin-top:3px;color:var(--muted);font-size:8px}.batch-file-actions{display:flex;gap:5px}.batch-emit-panel{display:grid;gap:10px;padding:12px;border:1px solid color-mix(in srgb,var(--primary) 40%,var(--line));border-radius:12px;background:color-mix(in srgb,var(--primary) 6%,var(--card))}.batch-emit-panel p{margin:0;color:var(--muted);font-size:9px;line-height:1.45}
    @media(max-width:760px){.order-file-context{grid-template-columns:1fr 1fr}.order-file-local,.date-mode{grid-column:1/-1}.order-file-toolbar{grid-template-columns:1fr}.order-file-summary{justify-content:flex-start}.order-file-row{grid-template-columns:minmax(0,1fr) 112px 116px;grid-template-areas:'product quantity unit';min-height:47px}.order-file-supplier{display:none}.order-file-control>span{display:none}.order-file-product strong{font-size:11.5px}.order-file-unit select,.quantity-enter-wrap input,.quantity-enter-button{min-height:39px!important}.batch-file-card{grid-template-columns:1fr}.batch-file-actions{display:grid;grid-template-columns:1fr 1fr}}
    @media(max-width:460px){.order-file-context{grid-template-columns:1fr}.date-input-row{grid-template-columns:minmax(0,1fr) 48px 62px}.order-file-row{grid-template-columns:minmax(0,1fr) 108px 96px}.quantity-enter-wrap{grid-template-columns:minmax(0,1fr) 36px}.supplier-date-row{grid-template-columns:1fr}.order-file-product small{font-size:6.5px}}
  `;document.head.append(style);
}

async function loadSources(){
  const [locations,centers,suppliers,products,categories,settings]=await Promise.all([api('/api/locations'),api('/api/cost-centers'),api('/api/suppliers'),api('/api/products'),api('/api/categories'),loadProcurementSettings(true)]);
  state.cache.locations=locations.locations||[];state.cache.costCenters=centers.costCenters||[];state.cache.suppliers=suppliers.suppliers||[];state.cache.products=products.products||[];state.cache.categories=categories.categories||[];settingsCache=settings;
}
function relationFor(product,selection){return(product.suppliers||[]).find(relation=>relation.id===selection?.relationId)||(product.suppliers||[])[0]||null}
function exactUnit(config,name){const normalized=String(name||'').trim().toUpperCase();return unitsForCenter(config).find(unit=>unit.name===normalized)||unitsForCenter(config)[0]||{name:'UNIDAD',unitsPerFormat:1}}
function unitOptions(config,selected){return unitsForCenter(config).map(unit=>`<option value="${esc(unit.name)}" ${unit.name===selected?'selected':''}>${esc(unit.name)}</option>`).join('')}
function supplierControl(product,selection){const relations=product.suppliers||[];if(relations.length<=1){const relation=relations[0];return `<div class="order-file-supplier-static">${esc(relation?.supplierName||'Sin proveedor')}</div><input type="hidden" data-order-relation value="${esc(relation?.id||'')}">`}return `<select data-order-relation>${relations.map(relation=>`<option value="${esc(relation.id)}" ${relation.id===selection.relationId?'selected':''}>${esc(relation.supplierName)}</option>`).join('')}</select>`}

async function openMasterOrder(){
  await loadSources();const locations=state.cache.locations;
  if(!locations.length)return toast('No tienes locales asignados','error');
  if(!state.cache.products.length)return toast('No hay productos en la lista maestra','error');
  const selections=new Map(),deliveryDates=new Map(),allowedCategories=new Map(state.cache.categories.map(category=>[category.id,category.name]));let activeProducts=[],activeConfig=null;
  const localControl=locations.length===1?`<div class="order-file-local"><span>Local</span><strong>${esc(locations[0].name)}</strong></div><input type="hidden" id="orderFileLocation" name="locationId" value="${esc(locations[0].id)}">`:`<label class="field"><span>Local</span><select id="orderFileLocation" name="locationId" required>${locations.map(location=>`<option value="${esc(location.id)}">${esc(location.name)}</option>`).join('')}</select></label>`;
  openModal({eyebrow:'ARCHIVO NUEVO',title:'Lista maestra',subtitle:'Guarda el archivo, revisa cada proveedor y presiona Emitir todo cuando esté terminado.',size:'order',body:`<div class="order-file-builder"><section class="order-file-context">${localControl}<label class="field"><span>Centro de costo</span><select id="orderFileCenter" name="costCenterId" required></select></label><div class="date-picker-box"><span class="field-label">Entrega general</span><div class="date-input-row"><input name="deliveryDate" id="orderFileGeneralDate" type="date" min="${today()}" value="${tomorrow()}" required><button class="btn date-shortcut" type="button" data-date-shortcut="today">Hoy</button><button class="btn date-shortcut" type="button" data-date-shortcut="tomorrow">Mañana</button></div></div><label class="date-mode"><span><strong>Fechas distintas por proveedor</strong><small>Actívalo para elegir una fecha diferente en cada pedido.</small></span><input id="orderFileIndividualDates" type="checkbox"></label><div class="supplier-date-list hidden" id="orderFileSupplierDates"></div></section><section class="order-file-toolbar"><label class="field"><span>Buscar</span><input id="orderFileSearch" placeholder="Producto, categoría o proveedor" enterkeyhint="search"></label><div class="order-file-summary"><strong id="orderFileSelectedCount">0</strong><span>productos · <b id="orderFileSupplierCount">0</b> proveedores</span></div></section><div id="orderFileProducts" class="order-file-list"></div><label class="field"><span>Notas para proveedores</span><textarea name="notes" placeholder="Observaciones opcionales"></textarea></label></div>`,submitLabel:'Guardar archivo',onSubmit:async form=>{
    saveVisible();const items=[];
    for(const product of activeProducts){const selection=selections.get(product.id);if(!selection||Number(selection.quantity)<=0)continue;const unit=exactUnit(activeConfig,selection.orderUnit);items.push({supplierProductId:selection.relationId,productId:product.id,quantity:Number(selection.quantity),orderUnit:unit.name,unitsPerOrderUnit:Number(unit.unitsPerFormat||1),persistFormat:true})}
    if(!items.length)throw new Error('Ingresa una cantidad en al menos un producto');
    const bySupplier={};if($('#orderFileIndividualDates').checked)$$('[data-supplier-delivery]').forEach(input=>{if(input.value)bySupplier[input.dataset.supplierDelivery]=input.value});
    const response=await api('/api/order-batches/v2',{method:'POST',headers:{'Idempotency-Key':crypto.randomUUID()},json:{locationId:form.get('locationId'),costCenterId:form.get('costCenterId'),deliveryDate:form.get('deliveryDate'),deliveryDates:bySupplier,notes:form.get('notes'),items}});
    state.cache.orders=[];toast('Archivo guardado y disponible para edición');await navigate('orders');setTimeout(()=>showSavedFile(response.batch),60);
  }});

  function currentLocationId(){return String($('#orderFileLocation')?.value||locations[0]?.id||'')}
  function centersForLocation(){return state.cache.costCenters.filter(center=>center.locationId===currentLocationId()&&center.active!==false)}
  function center(){return state.cache.costCenters.find(item=>item.id===$('#orderFileCenter').value)}
  function categoryNames(){return state.cache.categories.map(item=>item.name).filter(Boolean)}
  function initializeProducts(){
    const selectedCenter=center();if(!selectedCenter){activeProducts=[];render();return}
    activeConfig=configForCenter(selectedCenter,categoryNames(),settingsCache);
    activeProducts=state.cache.products.filter(product=>product.active&&(product.costCenters||[]).some(item=>item.id===selectedCenter.id)&&(product.suppliers||[]).length).map(product=>({...product,categoryName:allowedCategories.get(product.categoryId)||'Sin categoría'}));
    selections.clear();for(const product of activeProducts){const relation=product.suppliers[0],preferred=exactUnit(activeConfig,String(relation.orderUnit||'').toUpperCase());selections.set(product.id,{quantity:'',relationId:relation.id,orderUnit:preferred.name})}render();
  }
  function saveVisible(){$$('#orderFileProducts [data-order-product]').forEach(row=>{const selection=selections.get(row.dataset.orderProduct);if(!selection)return;selection.quantity=row.querySelector('[data-core-quantity]').value;selection.relationId=row.querySelector('[data-order-relation]').value;selection.orderUnit=row.querySelector('[data-order-unit]').value})}
  function selectedSuppliers(){const map=new Map();for(const product of activeProducts){const selection=selections.get(product.id);if(Number(selection?.quantity)<=0)continue;const relation=relationFor(product,selection);if(relation?.supplierId)map.set(relation.supplierId,relation.supplierName)}return map}
  function renderSupplierDates(){
    const container=$('#orderFileSupplierDates'),individual=$('#orderFileIndividualDates').checked,suppliers=selectedSuppliers();container.classList.toggle('hidden',!individual||!suppliers.size);
    if(!individual||!suppliers.size){container.innerHTML='';return}
    container.innerHTML=[...suppliers].map(([id,name])=>`<label class="supplier-date-row"><strong>${esc(name)}</strong><input type="date" min="${today()}" data-supplier-delivery="${esc(id)}" value="${esc(deliveryDates.get(id)||$('#orderFileGeneralDate').value||tomorrow())}"></label>`).join('');
    $$('[data-supplier-delivery]').forEach(input=>input.onchange=()=>deliveryDates.set(input.dataset.supplierDelivery,input.value));
  }
  function updateSummary(){saveVisible();const selected=activeProducts.filter(product=>Number(selections.get(product.id)?.quantity)>0),suppliers=selectedSuppliers();$('#orderFileSelectedCount').textContent=selected.length;$('#orderFileSupplierCount').textContent=suppliers.size;$$('#orderFileProducts [data-order-product]').forEach(row=>row.classList.toggle('selected',Number(selections.get(row.dataset.orderProduct)?.quantity)>0));renderSupplierDates()}
  function focusNext(current){const fields=$$('[data-core-quantity]').filter(input=>!input.disabled&&input.offsetParent!==null),next=fields[fields.indexOf(current)+1];if(next){next.focus({preventScroll:true});next.scrollIntoView({behavior:'smooth',block:'center'});setTimeout(()=>next.select(),60)}else $('#modalSubmit')?.focus()}
  function bindRows(){
    $$('#orderFileProducts [data-order-product]').forEach(row=>{const product=activeProducts.find(item=>item.id===row.dataset.orderProduct),quantity=row.querySelector('[data-core-quantity]'),relationInput=row.querySelector('[data-order-relation]'),unit=row.querySelector('[data-order-unit]');quantity.onfocus=()=>quantity.select();quantity.oninput=updateSummary;quantity.onkeydown=event=>{if(event.key!=='Enter')return;event.preventDefault();saveVisible();focusNext(quantity)};row.querySelector('[data-enter-next]').onclick=event=>{event.preventDefault();saveVisible();focusNext(quantity)};if(relationInput.tagName==='SELECT')relationInput.onchange=()=>{const relation=(product.suppliers||[]).find(item=>item.id===relationInput.value),selection=selections.get(product.id);selection.relationId=relationInput.value;selection.orderUnit=exactUnit(activeConfig,relation?.orderUnit).name;unit.value=selection.orderUnit;updateSummary()};unit.onchange=updateSummary});
  }
  function render(){
    saveVisible();const query=String($('#orderFileSearch')?.value||'').trim().toLowerCase(),visible=activeProducts.filter(product=>{const relation=relationFor(product,selections.get(product.id));return !query||`${product.name} ${product.categoryName||''} ${relation?.supplierName||''}`.toLowerCase().includes(query)}),groups=activeConfig?groupProductsByConfiguredOrder(visible,activeConfig):[];
    $('#orderFileProducts').innerHTML=groups.length?groups.map(({warehouse,categories})=>`<section class="order-file-warehouse"><div class="order-file-warehouse-title">${esc(warehouse.name)}</div>${categories.map(({category,items})=>`<section class="order-file-category"><div class="order-file-category-title">${esc(category)} · ${items.length}</div>${items.map(product=>{const selection=selections.get(product.id);return `<article class="order-file-row ${Number(selection.quantity)>0?'selected':''}" data-order-product="${esc(product.id)}"><div class="order-file-product"><strong>${esc(product.name)}</strong><small>${esc([product.brand,product.variant,product.categoryName].filter(Boolean).join(' · '))}</small></div><label class="order-file-control order-file-supplier"><span>Proveedor</span>${supplierControl(product,selection)}</label><label class="order-file-control order-file-unit"><span>Unidad</span><select data-order-unit>${unitOptions(activeConfig,selection.orderUnit)}</select></label><label class="order-file-control order-file-quantity"><span>Cantidad</span><div class="quantity-enter-wrap"><input data-core-quantity type="text" inputmode="decimal" enterkeyhint="next" value="${esc(selection.quantity)}" placeholder="0" autocomplete="off"><button class="btn quantity-enter-button" type="button" data-enter-next aria-label="Siguiente producto">↵</button></div></label></article>`}).join('')}</section>`).join('')}</section>`).join(''):'<div class="order-file-empty">No hay productos asignados a tus categorías para este centro. Configúralos en Operaciones.</div>';
    bindRows();updateSummary();
  }
  function refreshCenters(){const centers=centersForLocation();$('#orderFileCenter').innerHTML=centers.map(item=>`<option value="${esc(item.id)}">${esc(item.name)} · ${item.productCount} productos</option>`).join('');initializeProducts()}
  if(locations.length>1)$('#orderFileLocation').onchange=refreshCenters;$('#orderFileCenter').onchange=initializeProducts;$('#orderFileSearch').oninput=render;$('#orderFileIndividualDates').onchange=renderSupplierDates;$('#orderFileGeneralDate').onchange=renderSupplierDates;
  $$('[data-date-shortcut]').forEach(button=>button.onclick=()=>{$('#orderFileGeneralDate').value=button.dataset.dateShortcut==='today'?today():tomorrow();renderSupplierDates()});refreshCenters();
}

function showSavedFile(batch){
  openModal({eyebrow:'ARCHIVO GUARDADO',title:`${batch.supplierCount} pedidos en edición`,subtitle:'Revísalos. El proveedor no recibe nada hasta que presiones Emitir todo.',size:'large',hideSubmit:true,body:`<div class="batch-emit-panel"><p>Puedes editar cantidades, formatos o fechas por proveedor. Cuando esté correcto, emite todos los pedidos del archivo en un solo paso.</p><button class="btn primary wide" type="button" id="emitWholeBatch">Emitir todo</button></div><div class="batch-file-list">${batch.orders.map(order=>`<article class="batch-file-card"><div><strong>${esc(order.supplierName)} · ${esc(order.folio)}</strong><small>${Number(order.itemCount||0)} productos · entrega ${esc(order.deliveryDate||'sin fecha')}</small></div><div class="batch-file-actions"><button class="btn" type="button" data-edit-file-order="${esc(order.id)}">Editar</button><button class="btn danger" type="button" data-delete-file-order="${esc(order.id)}">Eliminar</button></div></article>`).join('')}</div>`});
  $$('[data-edit-file-order]').forEach(button=>button.onclick=()=>{closeModal('edit');setTimeout(()=>openOrderDetail(button.dataset.editFileOrder),0)});
  $$('[data-delete-file-order]').forEach(button=>button.onclick=async()=>{if(!confirm('¿Eliminar este pedido del archivo?'))return;await api(`/api/orders/${button.dataset.deleteFileOrder}`,{method:'DELETE'});button.closest('.batch-file-card')?.remove();toast('Pedido eliminado')});
  $('#emitWholeBatch').onclick=async()=>{if(!confirm('¿Emitir todos los pedidos? Después ya no podrán editarse.'))return;const button=$('#emitWholeBatch');button.disabled=true;button.textContent='Emitiendo…';try{await api(`/api/order-batches/${batch.batchId}/emit`,{method:'POST',json:{}});toast('Todos los pedidos fueron emitidos');closeModal('emitted');state.cache.orders=[];await navigate('orders')}catch(error){toast(error.message,'error');button.disabled=false;button.textContent='Emitir todo'}};
}

function intercept(event){const target=event.target.closest('button,[data-action]');if(!target)return;const isOrder=target.id==='mobileCreate'||target.dataset.action==='new-order'||(target.id==='primaryAction'&&['dashboard','orders'].includes(state.view));if(!isOrder)return;event.preventDefault();event.stopImmediatePropagation();openMasterOrder().catch(error=>toast(error.message,'error'))}
export function initializeOrderCoreV13(){if(initialized)return;initialized=true;injectStyles();document.addEventListener('click',intercept,true)}
