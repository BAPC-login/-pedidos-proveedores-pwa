const encoder=new TextEncoder();
const decoder=new TextDecoder();
const xml=value=>String(value??'').replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/"/g,'&quot;').replace(/'/g,'&apos;');

const SHEETS=[
  {name:'Proveedores',headers:['Proveedor','Razón social','RUT','Correo','Teléfono','Contacto','Días de entrega','Hora de corte','Pedido mínimo','Condiciones de pago']},
  {name:'Categorías',headers:['Local','Centro de costo','Categoría','Orden']},
  {name:'Centros de costo',headers:['Local','Código centro','Centro de costo','Activo']},
  {name:'Productos',headers:['Código producto','Producto','Marca','Variante','Contenido','Unidad contenido','Unidad base','Código de barras','Categoría','Centros de costo','Proveedor','SKU proveedor','Nombre producto proveedor','Unidad de compra','Unidades por formato','Precio bruto unitario']}
];

function crc32(bytes){let crc=0xffffffff;for(const byte of bytes){crc^=byte;for(let bit=0;bit<8;bit++)crc=(crc>>>1)^((crc&1)?0xedb88320:0)}return(crc^0xffffffff)>>>0}
function u16(value){return new Uint8Array([value&255,(value>>>8)&255])}
function u32(value){return new Uint8Array([value&255,(value>>>8)&255,(value>>>16)&255,(value>>>24)&255])}
function join(parts){const length=parts.reduce((sum,part)=>sum+part.length,0),output=new Uint8Array(length);let offset=0;for(const part of parts){output.set(part,offset);offset+=part.length}return output}
function columnName(index){let value=index+1,name='';while(value){value--;name=String.fromCharCode(65+(value%26))+name;value=Math.floor(value/26)}return name}
function sheetXml(headers){const cells=headers.map((header,index)=>`<c r="${columnName(index)}1" t="inlineStr" s="1"><is><t>${xml(header)}</t></is></c>`).join('');return`<?xml version="1.0" encoding="UTF-8" standalone="yes"?><worksheet xmlns="http://schemas.openxmlformats.org/spreadsheetml/2006/main"><sheetViews><sheetView workbookViewId="0"><pane ySplit="1" topLeftCell="A2" activePane="bottomLeft" state="frozen"/></sheetView></sheetViews><cols>${headers.map((header,index)=>`<col min="${index+1}" max="${index+1}" width="${Math.max(14,Math.min(34,header.length+5))}" customWidth="1"/>`).join('')}</cols><sheetData><row r="1" ht="22" customHeight="1">${cells}</row></sheetData><autoFilter ref="A1:${columnName(headers.length-1)}1"/></worksheet>`}
function createZip(entries){const locals=[],centrals=[];let offset=0;for(const entry of entries){const name=encoder.encode(entry.name),data=typeof entry.data==='string'?encoder.encode(entry.data):entry.data,crc=crc32(data),local=join([u32(0x04034b50),u16(20),u16(0x0800),u16(0),u16(0),u16(0),u32(crc),u32(data.length),u32(data.length),u16(name.length),u16(0),name,data]);locals.push(local);centrals.push(join([u32(0x02014b50),u16(20),u16(20),u16(0x0800),u16(0),u16(0),u16(0),u32(crc),u32(data.length),u32(data.length),u16(name.length),u16(0),u16(0),u16(0),u16(0),u32(0),u32(offset),name]));offset+=local.length}const central=join(centrals),end=join([u32(0x06054b50),u16(0),u16(0),u16(entries.length),u16(entries.length),u32(central.length),u32(offset),u16(0)]);return join([...locals,central,end])}

export function createWorkbookTemplate(){
  const workbookSheets=SHEETS.map((sheet,index)=>`<sheet name="${xml(sheet.name)}" sheetId="${index+1}" r:id="rId${index+1}"/>`).join('');
  const relationships=SHEETS.map((_,index)=>`<Relationship Id="rId${index+1}" Type="http://schemas.openxmlformats.org/officeDocument/2006/relationships/worksheet" Target="worksheets/sheet${index+1}.xml"/>`).join('');
  const overrides=SHEETS.map((_,index)=>`<Override PartName="/xl/worksheets/sheet${index+1}.xml" ContentType="application/vnd.openxmlformats-officedocument.spreadsheetml.worksheet+xml"/>`).join('');
  const entries=[
    {name:'[Content_Types].xml',data:`<?xml version="1.0" encoding="UTF-8" standalone="yes"?><Types xmlns="http://schemas.openxmlformats.org/package/2006/content-types"><Default Extension="rels" ContentType="application/vnd.openxmlformats-package.relationships+xml"/><Default Extension="xml" ContentType="application/xml"/><Override PartName="/xl/workbook.xml" ContentType="application/vnd.openxmlformats-officedocument.spreadsheetml.sheet.main+xml"/><Override PartName="/xl/styles.xml" ContentType="application/vnd.openxmlformats-officedocument.spreadsheetml.styles+xml"/>${overrides}</Types>`},
    {name:'_rels/.rels',data:'<?xml version="1.0" encoding="UTF-8" standalone="yes"?><Relationships xmlns="http://schemas.openxmlformats.org/package/2006/relationships"><Relationship Id="rId1" Type="http://schemas.openxmlformats.org/officeDocument/2006/relationships/officeDocument" Target="xl/workbook.xml"/></Relationships>'},
    {name:'xl/workbook.xml',data:`<?xml version="1.0" encoding="UTF-8" standalone="yes"?><workbook xmlns="http://schemas.openxmlformats.org/spreadsheetml/2006/main" xmlns:r="http://schemas.openxmlformats.org/officeDocument/2006/relationships"><sheets>${workbookSheets}</sheets></workbook>`},
    {name:'xl/_rels/workbook.xml.rels',data:`<?xml version="1.0" encoding="UTF-8" standalone="yes"?><Relationships xmlns="http://schemas.openxmlformats.org/package/2006/relationships">${relationships}<Relationship Id="rId${SHEETS.length+1}" Type="http://schemas.openxmlformats.org/officeDocument/2006/relationships/styles" Target="styles.xml"/></Relationships>`},
    {name:'xl/styles.xml',data:'<?xml version="1.0" encoding="UTF-8" standalone="yes"?><styleSheet xmlns="http://schemas.openxmlformats.org/spreadsheetml/2006/main"><fonts count="2"><font><sz val="11"/><name val="Aptos"/></font><font><b/><sz val="11"/><color rgb="FFFFFFFF"/><name val="Aptos"/></font></fonts><fills count="3"><fill><patternFill patternType="none"/></fill><fill><patternFill patternType="gray125"/></fill><fill><patternFill patternType="solid"><fgColor rgb="FF24116A"/><bgColor indexed="64"/></patternFill></fill></fills><borders count="1"><border><left/><right/><top/><bottom/><diagonal/></border></borders><cellStyleXfs count="1"><xf numFmtId="0" fontId="0" fillId="0" borderId="0"/></cellStyleXfs><cellXfs count="2"><xf numFmtId="0" fontId="0" fillId="0" borderId="0" xfId="0"/><xf numFmtId="0" fontId="1" fillId="2" borderId="0" xfId="0" applyFont="1" applyFill="1"/></cellXfs></styleSheet>'},
    ...SHEETS.map((sheet,index)=>({name:`xl/worksheets/sheet${index+1}.xml`,data:sheetXml(sheet.headers)}))
  ];
  return new Blob([createZip(entries)],{type:'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet'});
}
export function downloadWorkbookTemplate(){const url=URL.createObjectURL(createWorkbookTemplate()),anchor=document.createElement('a');anchor.href=url;anchor.download='plantilla-catalogo-compras.xlsx';document.body.append(anchor);anchor.click();anchor.remove();setTimeout(()=>URL.revokeObjectURL(url),3000)}

function findEnd(bytes){for(let index=bytes.length-22;index>=Math.max(0,bytes.length-65557);index--)if(bytes[index]===0x50&&bytes[index+1]===0x4b&&bytes[index+2]===0x05&&bytes[index+3]===0x06)return index;return-1}
const read16=(view,offset)=>view.getUint16(offset,true),read32=(view,offset)=>view.getUint32(offset,true);
async function inflate(data,method){if(method===0)return data;if(method!==8)throw new Error('El archivo Excel usa una compresión no compatible');if(typeof DecompressionStream!=='function')throw new Error('Este dispositivo no puede descomprimir el libro. Vuelve a guardarlo con Excel o actualiza iOS.');const stream=new Blob([data]).stream().pipeThrough(new DecompressionStream('deflate-raw'));return new Uint8Array(await new Response(stream).arrayBuffer())}
async function unzip(file){const bytes=new Uint8Array(await file.arrayBuffer()),view=new DataView(bytes.buffer,bytes.byteOffset,bytes.byteLength),end=findEnd(bytes);if(end<0)throw new Error('El archivo no es un libro Excel válido');const count=read16(view,end+10),centralOffset=read32(view,end+16),files=new Map();let cursor=centralOffset;for(let index=0;index<count;index++){if(read32(view,cursor)!==0x02014b50)throw new Error('El libro Excel está dañado');const method=read16(view,cursor+10),compressedSize=read32(view,cursor+20),nameLength=read16(view,cursor+28),extraLength=read16(view,cursor+30),commentLength=read16(view,cursor+32),localOffset=read32(view,cursor+42),name=decoder.decode(bytes.subarray(cursor+46,cursor+46+nameLength)),localNameLength=read16(view,localOffset+26),localExtraLength=read16(view,localOffset+28),dataOffset=localOffset+30+localNameLength+localExtraLength,compressed=bytes.subarray(dataOffset,dataOffset+compressedSize);files.set(name,await inflate(compressed,method));cursor+=46+nameLength+extraLength+commentLength}return files}
function parseXml(bytes,name){const document=new DOMParser().parseFromString(decoder.decode(bytes||new Uint8Array()),'application/xml');if(document.querySelector('parsererror'))throw new Error(`No se pudo leer ${name}`);return document}
function relationshipMap(document){const map=new Map();for(const node of document.querySelectorAll('Relationship'))map.set(node.getAttribute('Id'),node.getAttribute('Target'));return map}
function cellColumn(reference){const letters=String(reference||'A1').match(/[A-Z]+/i)?.[0]?.toUpperCase()||'A';let result=0;for(const char of letters)result=result*26+char.charCodeAt(0)-64;return result-1}
function sharedStrings(document){return[...document.querySelectorAll('si')].map(node=>[...node.querySelectorAll('t')].map(text=>text.textContent||'').join(''))}
function sheetRows(document,shared=[]){const output=[];for(const row of document.querySelectorAll('sheetData row')){const values=[];for(const cell of row.querySelectorAll('c')){const index=cellColumn(cell.getAttribute('r')),type=cell.getAttribute('t');let value='';if(type==='inlineStr')value=[...cell.querySelectorAll('is t')].map(node=>node.textContent||'').join('');else{const raw=cell.querySelector('v')?.textContent||'';value=type==='s'?shared[Number(raw)]??'':raw}values[index]=value}output.push(values)}return output}
function objectRows(rows){const headers=(rows[0]||[]).map(value=>String(value||'').trim());return rows.slice(1).filter(row=>row.some(value=>String(value||'').trim())).map(row=>Object.fromEntries(headers.map((header,index)=>[header,String(row[index]??'').trim()]).filter(([header])=>header)))}

export async function parseWorkbookFile(file){
  if(!(file instanceof File)||!file.size)throw new Error('Selecciona un archivo Excel');
  const files=await unzip(file),workbook=parseXml(files.get('xl/workbook.xml'),'el libro'),relationships=relationshipMap(parseXml(files.get('xl/_rels/workbook.xml.rels'),'las relaciones')),shared=files.has('xl/sharedStrings.xml')?sharedStrings(parseXml(files.get('xl/sharedStrings.xml'),'los textos compartidos')):[],result={};
  for(const sheet of workbook.querySelectorAll('sheets sheet')){const name=sheet.getAttribute('name')||'',id=sheet.getAttribute('r:id')||sheet.getAttributeNS('http://schemas.openxmlformats.org/officeDocument/2006/relationships','id'),target=relationships.get(id);if(!target)continue;const path=target.startsWith('/')?target.slice(1):`xl/${target.replace(/^\.\//,'')}`,bytes=files.get(path);if(!bytes)continue;result[name]=objectRows(sheetRows(parseXml(bytes,name),shared))}
  return result;
}
