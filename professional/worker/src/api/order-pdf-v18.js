import {listDocuments,archiveOrderPdf} from '../storage.js';
import {getOrder} from './orders.js';

function safeJson(value,fallback={}){try{return JSON.parse(value||'')}catch{return fallback}}
async function pdfIdentity(env,actor,order){const[supplier,organization]=await Promise.all([env.DB.prepare('SELECT settings_json FROM suppliers WHERE id=? AND org_id=?').bind(order.supplierId,actor.orgId).first(),env.DB.prepare('SELECT settings_json FROM organizations WHERE id=?').bind(actor.orgId).first()]),supplierIdentity=safeJson(supplier?.settings_json,{}).identity||{},branding=safeJson(organization?.settings_json,{}).branding||{};return{supplierLogoKey:String(supplierIdentity.logoKey||''),brandLogoKey:String(branding.logoKey||'')}}
export async function ensureOrderPdfV18(request,env,actor,orderId){const existing=await listDocuments(env,actor,{entityType:'order',entityId:orderId,kind:'order_pdf'}),order=await getOrder(env,actor,orderId),latest=existing.sort((a,b)=>Number(b.revision||0)-Number(a.revision||0)||String(b.createdAt||'').localeCompare(String(a.createdAt||'')))[0],identity=await pdfIdentity(env,actor,order),metadata=latest?.metadata||{},valid=latest&&Number(latest.revision||0)>=Number(order.revision||0)&&Number(metadata.pdfVersion||0)>=18&&String(metadata.status||'')===String(order.status||'')&&String(metadata.deliveryDate||'')===String(order.deliveryDate||'')&&String(metadata.supplierLogoKey||'')===identity.supplierLogoKey&&String(metadata.brandLogoKey||'')===identity.brandLogoKey&&String(metadata.costCenterName||'')===String(order.costCenterName||'');if(valid)return latest;return archiveOrderPdf(env,actor,order)}
