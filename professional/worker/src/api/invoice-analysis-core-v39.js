import aiWorker from '../../../../worker/src/index.js';
import {HttpError,ROLES,assertMinimumRole,monthKey,nowIso,planFor} from '../core.js';
import {writeAudit} from '../auth.js';
import {storeFile} from '../storage.js';
import {normalizeInvoiceAnalysis} from '../invoice-normalizer.js';

const ANALYSIS_TIMEOUT_MS=112000;
export const MAX_INVOICE_FILE_BYTES_V39=20*1024*1024;
const rows=result=>result?.results||[];
const normalizeKey=value=>String(value||'').normalize('NFD').replace(/[\u0300-\u036f]/g,'').toUpperCase().replace(/[^A-Z0-9]+/g,' ').replace(/\s+/g,' ').trim();
async function usageValue(env,orgId,metric){try{return Number((await env.DB.prepare('SELECT quantity FROM usage_counters WHERE org_id=? AND month_key=? AND metric=?').bind(orgId,monthKey(),metric).first())?.quantity||0)}catch(error){console.warn('invoice_usage_read_failed',error?.message||error);return 0}}
async function incrementUsage(env,orgId,metric,amount=1){try{await env.DB.prepare(`INSERT INTO usage_counters(org_id,month_key,metric,quantity,updated_at) VALUES(?,?,?,?,?) ON CONFLICT(org_id,month_key,metric) DO UPDATE SET quantity=usage_counters.quantity+excluded.quantity,updated_at=excluded.updated_at`).bind(orgId,monthKey(),metric,amount,nowIso()).run()}catch(error){console.warn('invoice_usage_write_failed',metric,error?.message||error)}}
async function recordClientEvent(env,actor,eventType,message,metadata={}){try{await env.DB.prepare(`INSERT INTO client_events(id,org_id,user_id,event_type,message,metadata_json,created_at) VALUES(?,?,?,?,?,?,?)`).bind(crypto.randomUUID(),actor.orgId,actor.userId,eventType,message,JSON.stringify(metadata),nowIso()).run()}catch(error){console.warn('invoice_analysis_event_failed',error?.message||error)}}
function timeoutPromise(){return new Promise((_,reject)=>setTimeout(()=>reject(Object.assign(new Error('Nuvasto agotó el tiempo de lectura del documento.'),{code:'analysis_timeout'})),ANALYSIS_TIMEOUT_MS))}
async function callInternalAnalysis(env,{file,orderFile,context}){const form=new FormData();form.append('file',file,file.name||'documento');if(orderFile instanceof File&&orderFile.size)form.append('orderFile',orderFile,orderFile.name||'pedido.pdf');form.append('context',JSON.stringify(context));const response=await Promise.race([aiWorker.fetch(new Request('https://nuvasto.internal/v1/invoices/analyze',{method:'POST',body:form}),env),timeoutPromise()]),payload=await response.json().catch(()=>({}));if(response.ok&&payload.ok)return payload;const error=new Error(payload.error||'Nuvasto no pudo interpretar el documento');error.code=payload.code||'analysis_failed';error.status=response.status||502;error.details={attempts:payload.attempts||[]};throw error}

function mergeAliases(browserAliases,historicalAliases){const map=new Map();for(const raw of [...(Array.isArray(browserAliases)?browserAliases:[]),...(Array.isArray(historicalAliases)?historicalAliases:[])]){const productId=String(raw.productId||raw.product_id||''),alias=String(raw.alias||raw.source_description||'').trim();if(!productId||!alias)continue;const key=`${productId}|${normalizeKey(alias)}`,existing=map.get(key);const candidate={productId,alias,confidence:Number(raw.confidence??raw.match_confidence??0)||0,usageCount:Number(raw.usageCount??raw.usage_count??1)||1};if(!existing||candidate.confidence>existing.confidence||candidate.usageCount>existing.usageCount)map.set(key,candidate)}return[...map.values()].slice(0,700)}
async function canonicalReconciliationContext(env,actor,rawContext){
  const context=rawContext&&typeof rawContext==='object'?rawContext:{};
  const requestedOrderId=String(context.orderId||'').trim(),folio=String(context.folio||'').trim();
  if(!requestedOrderId&&!folio)return{...context,reconciliationContextSource:'browser-fallback'};
  try{
    const order=requestedOrderId
      ?await env.DB.prepare(`SELECT o.id,o.supplier_id,s.name supplier_name FROM orders o JOIN suppliers s ON s.id=o.supplier_id AND s.org_id=o.org_id WHERE o.org_id=? AND o.id=?`).bind(actor.orgId,requestedOrderId).first()
      :await env.DB.prepare(`SELECT o.id,o.supplier_id,s.name supplier_name FROM orders o JOIN suppliers s ON s.id=o.supplier_id AND s.org_id=o.org_id WHERE o.org_id=? AND o.folio=? ORDER BY o.created_at DESC LIMIT 1`).bind(actor.orgId,folio).first();
    if(!order)return{...context,reconciliationContextSource:'browser-order-not-found'};
    const itemResult=await env.DB.prepare(`SELECT oi.product_id,oi.supplier_product_id,oi.description_snapshot,oi.quantity_ordered,oi.order_unit_snapshot,oi.units_per_order_unit,
      p.name catalog_name,p.barcode,sp.supplier_sku,sp.supplier_name supplier_product_name
      FROM order_items oi
      LEFT JOIN products p ON p.id=oi.product_id
      LEFT JOIN supplier_products sp ON sp.id=oi.supplier_product_id
      WHERE oi.order_id=? ORDER BY oi.sort_order,oi.created_at`).bind(order.id).all();
    const products=rows(itemResult).filter(item=>item.product_id).map(item=>({
      productId:item.product_id,
      supplierProductId:item.supplier_product_id||'',
      description:item.description_snapshot||item.catalog_name||item.supplier_product_name||'',
      catalogName:item.catalog_name||item.description_snapshot||'',
      supplierProductName:item.supplier_product_name||'',
      supplierSku:item.supplier_sku||'',
      barcode:item.barcode||'',
      unit:item.order_unit_snapshot||'UNIDAD',
      orderedQty:Number(item.quantity_ordered||0),
      unitsPerOrderUnit:Math.max(1,Number(item.units_per_order_unit||1))
    }));
    if(!products.length)return{...context,orderId:order.id,supplierId:order.supplier_id,providerName:context.providerName||order.supplier_name,reconciliationContextSource:'browser-empty-order'};
    let historical=[];
    try{
      const history=await env.DB.prepare(`SELECT il.product_id,il.source_description alias,AVG(COALESCE(il.match_confidence,0)) confidence,COUNT(*) usage_count
        FROM invoice_lines il JOIN invoices i ON i.id=il.invoice_id
        WHERE i.org_id=? AND i.supplier_id=? AND i.status<>'void' AND il.product_id IS NOT NULL AND TRIM(COALESCE(il.source_description,''))<>''
          AND EXISTS(SELECT 1 FROM order_items oi2 WHERE oi2.order_id=? AND oi2.product_id=il.product_id)
        GROUP BY il.product_id,UPPER(TRIM(il.source_description)) ORDER BY usage_count DESC LIMIT 500`).bind(actor.orgId,order.supplier_id,order.id).all();
      historical=rows(history).map(item=>({productId:item.product_id,alias:item.alias,confidence:Math.max(.5,Number(item.confidence||0)),usageCount:Number(item.usage_count||1)}));
    }catch(error){console.warn('invoice_historical_aliases_failed',error?.message||error)}
    const aliases=mergeAliases(context.aliases,historical);
    return{...context,orderId:order.id,supplierId:order.supplier_id,providerName:context.providerName||order.supplier_name,products,aliases,reconciliationContextSource:'server-order-supplier-evidence-v75',serverCatalogProductCount:products.length,serverAliasCount:aliases.length};
  }catch(error){console.warn('invoice_reconciliation_context_failed',error?.message||error);return{...context,reconciliationContextSource:'browser-context-error'}}
}

function manualReviewLines(context){return(context.products||[]).map((product,index)=>{const pack=Math.max(1,Number(product.unitsPerOrderUnit||product.pack||1));return{id:`manual-${index+1}`,code:'',sourceLine:String(product.description||`Producto ${index+1}`),descriptionOriginal:String(product.description||`Producto ${index+1}`),description:String(product.description||`Producto ${index+1}`),productId:String(product.productId||''),suggestedProductId:String(product.productId||''),invoiceQuantity:0,packageQty:0,packSize:pack,units:0,totalUnits:0,orderPackSize:pack,receivedOrderQty:0,grossLineTotal:0,grossUnitPrice:0,grossPackPrice:0,confidence:.25,matchMethod:'nuvasto_manual_review',matchReason:'Producto precargado desde el pedido para revisión manual',quantityStatus:'review',conversionSummary:`Pedido: ${Number(product.orderedQty||0)} ${String(product.unit||'UNIDAD')}. Completa lo leído en el documento.`,notes:'La lectura automática no entregó una línea confiable.',isFree:false,freeReason:'',engine:'nuvasto-manual'}})}
function degradedAnalysis(context,sourceFile,error,elapsedMs){const lines=manualReviewLines(context);return{model:'nuvasto-review-fallback',sourceFile,degraded:true,providerErrorCode:error?.code||'analysis_failed',elapsedMs,warnings:['Nuvasto guardó el documento, pero la lectura automática no entregó datos confiables.','Los productos del pedido fueron precargados para que puedas completar cantidades y precios sin volver a subir el archivo.'],invoice:{supplierName:String(context.providerName||''),supplierRut:'',invoiceNumber:'',invoiceDate:'',currency:'CLP',documentType:'',documentTypeCode:'33',totals:{net:0,freight:0,additionalTax:0,vat:0,other:0,total:0},lines,items:lines,matchSummary:{matched:lines.length,unmatched:0,totalInvoiceLines:lines.length,freeLines:0},warnings:['Lectura automática incompleta. Revisa manualmente los valores antes de guardar.']}}}
const analysisLines=analysis=>analysis?.invoice?.lines||analysis?.invoice?.items||[];
export async function analyzeInvoiceCoreV39(request,env,actor){assertMinimumRole(actor.role,ROLES.RECEIVER);if(!env.FILES)throw new HttpError(503,'Nuvasto no puede almacenar el documento porque R2 no está disponible.','r2_unavailable');const limits=planFor(actor.organization.plan),used=await usageValue(env,actor.orgId,'ai_documents');if(used>=limits.aiDocumentsPerMonth)throw new HttpError(402,'Se alcanzó el límite mensual de documentos del plan.','plan_limit');const form=await request.formData(),file=form.get('file');if(!(file instanceof File)||!file.size)throw new HttpError(400,'Adjunta una factura, guía, boleta o nota de crédito.','missing_file');if(file.size>MAX_INVOICE_FILE_BYTES_V39)throw new HttpError(413,'El documento supera 20 MB.','file_too_large');const orderFile=form.get('orderFile');let context={};try{context=JSON.parse(String(form.get('context')||'{}'))}catch{throw new HttpError(400,'El contexto del pedido no es válido.','invalid_context')}context=await canonicalReconciliationContext(env,actor,context);context={...context,organizationId:actor.orgId,requestedBy:actor.userId,normalizationVersion:71,flowVersion:39,brand:'Nuvasto'};const analysisId=crypto.randomUUID(),sourceFile=await storeFile(env,actor,file,{purpose:'invoice-source'});await incrementUsage(env,actor.orgId,'file_bytes',file.size);const started=Date.now();try{const raw=await callInternalAnalysis(env,{file,orderFile,context}),normalized=normalizeInvoiceAnalysis({...raw,sourceFile},context),lines=analysisLines(normalized);if(!lines.length)throw Object.assign(new Error('Nuvasto no detectó líneas de productos en el documento.'),{code:'invoice_lines_not_detected'});const elapsedMs=Date.now()-started,matched=lines.filter(line=>line.productId).length;Object.assign(normalized,{elapsedMs,degraded:false,flowVersion:39,maxFileBytes:MAX_INVOICE_FILE_BYTES_V39,priceReconciliation:'deterministic-line-arithmetic',reconciliationEngine:'supplier-evidence-v75',reconciliationContextSource:context.reconciliationContextSource});await incrementUsage(env,actor.orgId,'ai_documents',1);await recordClientEvent(env,actor,'invoice.analysis.success','Documento leído y cotejado',{analysisId,folio:String(context.folio||''),orderId:String(context.orderId||''),fileName:file.name,fileSize:file.size,lineCount:lines.length,matched,unmatched:lines.length-matched,priceVerified:lines.filter(line=>line.priceVerified).length,elapsedMs,model:normalized.model||raw.model||'',sourceFileId:sourceFile.id,flowVersion:39,normalizationVersion:71,reconciliationEngine:'supplier-evidence-v75',reconciliationContextSource:context.reconciliationContextSource,serverCatalogProductCount:Number(context.serverCatalogProductCount||0),serverAliasCount:Number(context.serverAliasCount||0)});await writeAudit(env,actor,request,'invoice.analyze','invoice-analysis',analysisId,{status:'success',fileName:file.name,fileSize:file.size,sourceFileId:sourceFile.id,flowVersion:39,normalizationVersion:71,folio:String(context.folio||''),orderId:String(context.orderId||''),lineCount:lines.length,matched,priceVerified:lines.filter(line=>line.priceVerified).length,elapsedMs,model:normalized.model||raw.model||'',reconciliationEngine:'supplier-evidence-v75',reconciliationContextSource:context.reconciliationContextSource});return normalized}catch(error){const elapsedMs=Date.now()-started,fallback=degradedAnalysis(context,sourceFile,error,elapsedMs);fallback.flowVersion=39;fallback.maxFileBytes=MAX_INVOICE_FILE_BYTES_V39;fallback.normalizationVersion=71;fallback.reconciliationEngine='supplier-evidence-v75';fallback.reconciliationContextSource=context.reconciliationContextSource;await incrementUsage(env,actor.orgId,'ai_documents',1);await recordClientEvent(env,actor,'invoice.analysis.degraded','Lectura automática derivada a revisión manual',{analysisId,folio:String(context.folio||''),orderId:String(context.orderId||''),fileName:file.name,fileSize:file.size,elapsedMs,errorCode:error?.code||'analysis_failed',errorMessage:String(error?.message||error),attempts:error?.details?.attempts||[],sourceFileId:sourceFile.id,flowVersion:39,reconciliationEngine:'supplier-evidence-v75',reconciliationContextSource:context.reconciliationContextSource});await writeAudit(env,actor,request,'invoice.analyze','invoice-analysis',analysisId,{status:'degraded',fileName:file.name,fileSize:file.size,sourceFileId:sourceFile.id,flowVersion:39,folio:String(context.folio||''),orderId:String(context.orderId||''),elapsedMs,errorCode:error?.code||'analysis_failed',reconciliationEngine:'supplier-evidence-v75'});return fallback}}
export async function invoiceAnalysisMetricsV39(env,actor){assertMinimumRole(actor.role,ROLES.ADMIN);const result=await env.DB.prepare(`SELECT event_type,metadata_json,created_at FROM client_events WHERE org_id=? AND event_type IN ('invoice.analysis.success','invoice.analysis.degraded') ORDER BY created_at DESC LIMIT 500`).bind(actor.orgId).all(),events=rows(result).map(row=>{let metadata={};try{metadata=JSON.parse(row.metadata_json||'{}')}catch{}return{type:row.event_type,metadata,createdAt:row.created_at}}),total=events.length,success=events.filter(item=>item.type.endsWith('.success')).length,elapsed=events.map(item=>Number(item.metadata.elapsedMs||0)).filter(Boolean);return{total,success,degraded:total-success,successRate:total?Number((success/total*100).toFixed(1)):0,averageMs:elapsed.length?Math.round(elapsed.reduce((sum,value)=>sum+value,0)/elapsed.length):0,recent:events.slice(0,50)}}
