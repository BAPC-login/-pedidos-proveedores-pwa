const PT_PER_MM=72/25.4;
const PAGE={width:210*PT_PER_MM,height:297*PT_PER_MM};
const encoder=new TextEncoder();
const cp1252={0x20ac:128,0x201a:130,0x0192:131,0x201e:132,0x2026:133,0x2020:134,0x2021:135,0x02c6:136,0x2030:137,0x0160:138,0x2039:139,0x0152:140,0x017d:142,0x2018:145,0x2019:146,0x201c:147,0x201d:148,0x2022:149,0x2013:150,0x2014:151,0x02dc:152,0x2122:153,0x0161:154,0x203a:155,0x0153:156,0x017e:158,0x0178:159};

function concat(parts){const size=parts.reduce((sum,part)=>sum+part.length,0),output=new Uint8Array(size);let offset=0;for(const part of parts){output.set(part,offset);offset+=part.length}return output}
function pdfEscape(value){let output='';for(const character of String(value??'')){const code=character.codePointAt(0),byte=code<=255?code:(cp1252[code]??63);if(byte===40||byte===41||byte===92)output+=`\\${String.fromCharCode(byte)}`;else if(byte<32||byte>126)output+=`\\${byte.toString(8).padStart(3,'0')}`;else output+=String.fromCharCode(byte)}return output}
const fmt=value=>Number(Number(value).toFixed(3)).toString();
const mm=value=>value*PT_PER_MM;
function hexRgb(hex){const value=String(hex||'#000000').replace('#','').padEnd(6,'0').slice(0,6);return[parseInt(value.slice(0,2),16)||0,parseInt(value.slice(2,4),16)||0,parseInt(value.slice(4,6),16)||0]}
function contrast(hex){const[r,g,b]=hexRgb(hex);return(r*299+g*587+b*114)/1000>155?'#111111':'#FFFFFF'}
function estimateWidth(text,size,bold=false){let total=0;for(const character of String(text??'')){if('MW@%'.includes(character))total+=.82;else if('ilI1.,:;|'.includes(character))total+=.28;else if(character===' ')total+=.28;else total+=bold?.57:.52}return total*size}
function wrapText(text,maxWidth,size,bold=false){const lines=[];for(const paragraph of String(text??'').split(/\n/)){const words=paragraph.split(/\s+/).filter(Boolean);if(!words.length){lines.push('');continue}let line='';for(const word of words){const next=line?`${line} ${word}`:word;if(estimateWidth(next,size,bold)<=maxWidth){line=next;continue}if(line)lines.push(line);if(estimateWidth(word,size,bold)<=maxWidth){line=word;continue}let chunk='';for(const character of word){if(estimateWidth(chunk+character,size,bold)>maxWidth&&chunk){lines.push(chunk);chunk=character}else chunk+=character}line=chunk}if(line)lines.push(line)}return lines}
function jpegDimensions(bytes){const data=bytes instanceof Uint8Array?bytes:new Uint8Array(bytes||[]);if(data.length<4||data[0]!==0xff||data[1]!==0xd8)return null;let offset=2;while(offset+9<data.length){if(data[offset]!==0xff){offset++;continue}const marker=data[offset+1];offset+=2;if(marker===0xd8||marker===0xd9)continue;if(offset+2>data.length)break;const length=(data[offset]<<8)+data[offset+1];if(length<2||offset+length>data.length)break;if([0xc0,0xc1,0xc2,0xc3,0xc5,0xc6,0xc7,0xc9,0xca,0xcb,0xcd,0xce,0xcf].includes(marker))return{height:(data[offset+3]<<8)+data[offset+4],width:(data[offset+5]<<8)+data[offset+6]};offset+=length}return null}

class Page{constructor(){this.ops=[];this.images=new Set()}push(value){this.ops.push(value)}}
class Document{
  constructor(){this.pages=[new Page()];this.pageIndex=0;this.images=[]}
  get page(){return this.pages[this.pageIndex]}
  addPage(){this.pages.push(new Page());this.pageIndex=this.pages.length-1;return this}
  setStroke(hex='#000000'){const[r,g,b]=hexRgb(hex);this.page.push(`${fmt(r/255)} ${fmt(g/255)} ${fmt(b/255)} RG`);return this}
  setFill(hex='#000000'){const[r,g,b]=hexRgb(hex);this.page.push(`${fmt(r/255)} ${fmt(g/255)} ${fmt(b/255)} rg`);return this}
  line(x1,y1,x2,y2,width=.25){this.page.push(`${fmt(mm(width))} w ${fmt(mm(x1))} ${fmt(PAGE.height-mm(y1))} m ${fmt(mm(x2))} ${fmt(PAGE.height-mm(y2))} l S`);return this}
  rect(x,y,width,height,mode='S',lineWidth=.25){this.page.push(`${fmt(mm(lineWidth))} w ${fmt(mm(x))} ${fmt(PAGE.height-mm(y+height))} ${fmt(mm(width))} ${fmt(mm(height))} re ${mode}`);return this}
  text(text,x,y,{size=9,bold=false,align='left',maxWidth=0,lineHeight=1.18,color='#000000'}={}){const font=bold?'F2':'F1',[r,g,b]=hexRgb(color),width=maxWidth?mm(maxWidth):0,lines=maxWidth?wrapText(text,width,size,bold):String(text??'').split('\n');lines.forEach((line,index)=>{let xx=mm(x);const textWidth=estimateWidth(line,size,bold);if(align==='center')xx-=textWidth/2;else if(align==='right')xx-=textWidth;const yy=PAGE.height-mm(y)-index*size*lineHeight;this.page.push(`BT /${font} ${fmt(size)} Tf ${fmt(r/255)} ${fmt(g/255)} ${fmt(b/255)} rg 1 0 0 1 ${fmt(xx)} ${fmt(yy)} Tm (${pdfEscape(line)}) Tj ET`)});return lines.length}
  addJpeg(bytes,width,height){const name=`Im${this.images.length+1}`;this.images.push({name,bytes:bytes instanceof Uint8Array?bytes:new Uint8Array(bytes),width,height});return name}
  image(name,x,y,width,height){const image=this.images.find(entry=>entry.name===name);if(!image)return;const ratio=image.width/image.height;let drawWidth=width,drawHeight=height;if(drawWidth/drawHeight>ratio)drawWidth=drawHeight*ratio;else drawHeight=drawWidth/ratio;this.page.images.add(name);this.page.push(`q ${fmt(mm(drawWidth))} 0 0 ${fmt(mm(drawHeight))} ${fmt(mm(x))} ${fmt(PAGE.height-mm(y+drawHeight))} cm /${name} Do Q`)}
  bytes(){const objects=[],add=value=>{objects.push(value);return objects.length};add(null);add(null);const regular=add(encoder.encode('<< /Type /Font /Subtype /Type1 /BaseFont /Helvetica /Encoding /WinAnsiEncoding >>')),bold=add(encoder.encode('<< /Type /Font /Subtype /Type1 /BaseFont /Helvetica-Bold /Encoding /WinAnsiEncoding >>')),imageRefs={};for(const image of this.images){const head=encoder.encode(`<< /Type /XObject /Subtype /Image /Width ${image.width} /Height ${image.height} /ColorSpace /DeviceRGB /BitsPerComponent 8 /Filter /DCTDecode /Length ${image.bytes.length} >>\nstream\n`);imageRefs[image.name]=add(concat([head,image.bytes,encoder.encode('\nendstream')]))}const pageRefs=[];for(const page of this.pages){const content=encoder.encode(page.ops.join('\n')),contentRef=add(concat([encoder.encode(`<< /Length ${content.length} >>\nstream\n`),content,encoder.encode('\nendstream')])),xObjects=[...page.images].map(name=>`/${name} ${imageRefs[name]} 0 R`).join(' '),resources=`<< /Font << /F1 ${regular} 0 R /F2 ${bold} 0 R >>${xObjects?` /XObject << ${xObjects} >>`:''} >>`;pageRefs.push(add(encoder.encode(`<< /Type /Page /Parent 2 0 R /MediaBox [0 0 ${fmt(PAGE.width)} ${fmt(PAGE.height)}] /Resources ${resources} /Contents ${contentRef} 0 R >>`)))}objects[1]=encoder.encode(`<< /Type /Pages /Kids [${pageRefs.map(number=>`${number} 0 R`).join(' ')}] /Count ${pageRefs.length} >>`);objects[0]=encoder.encode('<< /Type /Catalog /Pages 2 0 R >>');const header=encoder.encode('%PDF-1.4\n%PedidosPro\n'),parts=[header],offsets=[0];let offset=header.length;for(let index=0;index<objects.length;index++){offsets[index+1]=offset;const block=concat([encoder.encode(`${index+1} 0 obj\n`),objects[index],encoder.encode('\nendobj\n')]);parts.push(block);offset+=block.length}const xrefOffset=offset;let xref=`xref\n0 ${objects.length+1}\n0000000000 65535 f \n`;for(let index=1;index<=objects.length;index++)xref+=`${String(offsets[index]).padStart(10,'0')} 00000 n \n`;xref+=`trailer\n<< /Size ${objects.length+1} /Root 1 0 R >>\nstartxref\n${xrefOffset}\n%%EOF`;parts.push(encoder.encode(xref));return concat(parts)}
}

const valueOrDash=value=>String(value||'').trim()||'—';
function localAddress(business,location){const details=location?.details||{};return[details.address||business.address,details.commune||business.commune,details.city||business.city].filter(Boolean).join(', ')}
function makeImage(doc,bytes,fallback={}){if(!bytes?.length)return null;const dimensions=jpegDimensions(bytes)||{width:Number(fallback.width||0),height:Number(fallback.height||0)};if(dimensions.width<=0||dimensions.height<=0)return null;return{...dimensions,name:doc.addJpeg(bytes,dimensions.width,dimensions.height)}}
function fitLogo(doc,image,box,alignment={}){if(!image?.name)return;const ratio=image.width/image.height;let width=Math.min(box.w,Math.max(10,Number(alignment.size||box.w))),height=width/ratio;if(height>box.h){height=box.h;width=height*ratio}let x=box.x,y=box.y;if(alignment.x==='right')x+=box.w-width;else if(alignment.x==='center')x+=(box.w-width)/2;if(alignment.y==='bottom')y+=box.h-height;else if(alignment.y==='center')y+=(box.h-height)/2;doc.image(image.name,x,y,width,height)}
function drawInfoRows(doc,x,y,width,rows){const rowHeight=6,labelWidth=Math.min(42,Math.max(31,width*.3));rows.forEach((row,index)=>{const yy=y+index*rowHeight;doc.setStroke('#30333A').rect(x,yy,labelWidth,rowHeight).rect(x+labelWidth,yy,width-labelWidth,rowHeight);doc.text(row[0],x+1.3,yy+4.15,{size:6,bold:true});let size=7;while(estimateWidth(row[1],size)>mm(width-labelWidth-3)&&size>4.7)size-=.2;doc.text(row[1],x+labelWidth+1.4,yy+4.15,{size})});return rows.length*rowHeight}
function drawHeader(doc,context,companyLogo){const{organization,business,branding,location,order,requester}=context,x=15,y=10,width=180,rows=[['RAZÓN SOCIAL',valueOrDash(location?.details?.legalName||business.legalName||organization.name)],['RUT',valueOrDash(location?.details?.rut||business.rut)],['DIRECCIÓN',valueOrDash(localAddress(business,location))],['LOCAL',valueOrDash(location?.name||order.locationName)],['SOLICITADO POR',valueOrDash([requester.displayName,requester.profile?.jobTitle].filter(Boolean).join(' · '))],['FOLIO',valueOrDash(order.folio)]],tableHeight=rows.length*6;if(!companyLogo?.name){drawInfoRows(doc,x,y,width,rows);return y+tableHeight}const position=branding.logoPosition||'left',alignment={x:branding.logoAlignX||'center',y:branding.logoAlignY||'center',size:branding.logoSize||42};if(position==='top'||position==='bottom'){const logoHeight=Math.max(20,Math.min(38,Number(branding.logoSize||42)*.55));doc.setStroke('#30333A').rect(x,y,width,tableHeight+logoHeight);if(position==='top'){fitLogo(doc,companyLogo,{x:x+4,y:y+3,w:width-8,h:logoHeight-6},alignment);doc.line(x,y+logoHeight,x+width,y+logoHeight);drawInfoRows(doc,x,y+logoHeight,width,rows)}else{drawInfoRows(doc,x,y,width,rows);doc.line(x,y+tableHeight,x+width,y+tableHeight);fitLogo(doc,companyLogo,{x:x+4,y:y+tableHeight+3,w:width-8,h:logoHeight-6},alignment)}return y+tableHeight+logoHeight}const logoWidth=Math.max(42,Math.min(72,Number(branding.logoSize||42)+13)),infoWidth=width-logoWidth;doc.setStroke('#30333A').rect(x,y,width,tableHeight);if(position==='right'){drawInfoRows(doc,x,y,infoWidth,rows);doc.line(x+infoWidth,y,x+infoWidth,y+tableHeight);fitLogo(doc,companyLogo,{x:x+infoWidth+4,y:y+4,w:logoWidth-8,h:tableHeight-8},alignment)}else{fitLogo(doc,companyLogo,{x:x+4,y:y+4,w:logoWidth-8,h:tableHeight-8},alignment);doc.line(x+logoWidth,y,x+logoWidth,y+tableHeight);drawInfoRows(doc,x+logoWidth,y,infoWidth,rows)}return y+tableHeight}
function drawSupplierBand(doc,y,context,supplierLogo){const color=context.branding.primaryColor||'#6246EA',textColor=contrast(color),hasLogo=Boolean(supplierLogo?.name),logoWidth=hasLogo?30:0;doc.setFill(color).rect(15,y,180,12,'F');doc.text(`PEDIDO A ${String(context.order.supplierName||'PROVEEDOR').toUpperCase()}`,15+(180-logoWidth)/2,y+7.8,{size:12,bold:true,align:'center',color:textColor,maxWidth:145});if(hasLogo){doc.setFill('#FFFFFF').rect(164,y+1,29,10,'F');fitLogo(doc,supplierLogo,{x:166,y:y+2,w:25,h:8},{x:'center',y:'center',size:Number(context.supplierBranding?.logoSize||25)})}return y+16}
function drawMeta(doc,y,context){const order=context.order,entries=[['Centro de costo',order.costCenterName||'Barra'],['Entrega esperada',order.deliveryDate?new Date(`${order.deliveryDate}T12:00:00`).toLocaleDateString('es-CL'):'Sin fecha']],slot=90;entries.forEach((entry,index)=>{const x=15+slot*index;doc.setStroke('#D0D3DA').rect(x,y,slot,11);doc.text(entry[0].toUpperCase(),x+2,y+3.8,{size:5.8,bold:true,color:'#636A78'});doc.text(valueOrDash(entry[1]),x+2,y+8.6,{size:7.5,bold:true,maxWidth:slot-4})});return y+15}
function drawTableHeader(doc,y,context){const color=context.branding.tableHeaderColor||'#48484C',textColor=contrast(color);doc.setFill(color).rect(15,y,180,8,'F');doc.text('DESCRIPCIÓN',18,y+5.5,{size:8,bold:true,color:textColor});doc.text('CANTIDAD',154,y+5.5,{size:8,bold:true,align:'center',color:textColor});doc.text('FORMATO',169,y+5.5,{size:8,bold:true,color:textColor});return y+8}
function drawFooter(doc,context,pageNumber){const footer=context.branding.footerText||'Documento generado por Pedidos Pro';doc.line(15,287,195,287,.2);doc.text(footer,15,291.5,{size:6.4,color:'#697181'});doc.text(`Página ${pageNumber}`,195,291.5,{size:6.4,align:'right',color:'#697181'})}

export function createProfessionalOrderPdfV2(context){const doc=new Document(),companyLogo=makeImage(doc,context.logoBytes,{width:context.branding.logoWidth,height:context.branding.logoHeight}),supplierLogo=makeImage(doc,context.supplierLogoBytes,{width:context.supplierBranding?.logoWidth,height:context.supplierBranding?.logoHeight});let pageNumber=1,y=drawHeader(doc,context,companyLogo)+5;y=drawSupplierBand(doc,y,context,supplierLogo);y=drawMeta(doc,y,context);y=drawTableHeader(doc,y,context);for(const item of context.order.items||[]){const description=wrapText(item.description,mm(123),8.2),format=wrapText(item.orderUnit||'UNIDAD',mm(25),8.2),rowHeight=Math.max(8,Math.max(description.length,format.length)*3.8+2.6);if(y+rowHeight>282){drawFooter(doc,context,pageNumber);doc.addPage();pageNumber++;y=15;y=drawTableHeader(doc,y,context)}doc.setStroke('#30333A').rect(15,y,128,rowHeight).rect(143,y,22,rowHeight).rect(165,y,30,rowHeight);doc.text(description.join('\n'),18,y+5.2,{size:8.2});doc.text(String(item.quantityOrdered),161,y+5.2,{size:8.4,bold:true,align:'right'});doc.text(format.join('\n'),168,y+5.2,{size:8.2});y+=rowHeight}if(context.order.notes){const noteLines=wrapText(`Observaciones: ${context.order.notes}`,mm(172),7.5),noteHeight=Math.max(12,noteLines.length*3.5+5);if(y+noteHeight>282){drawFooter(doc,context,pageNumber);doc.addPage();pageNumber++;y=15}doc.setStroke('#AEB4BF').rect(15,y+4,180,noteHeight);doc.text(noteLines.join('\n'),18,y+10,{size:7.5})}drawFooter(doc,context,pageNumber);return doc.bytes()}
