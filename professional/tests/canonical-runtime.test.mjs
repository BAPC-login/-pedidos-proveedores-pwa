import assert from 'node:assert/strict';import fs from 'node:fs';const read=path=>fs.readFileSync(path,'utf8');
const app=read('web/app.js'),entry=read('web/app-professional.js'),runtime=read('web/app-runtime.js'),mobile=read('web/app-mobile-runtime.js'),bootstrap=read('web/app-bootstrap.js'),files=read('web/app-file-actions.js'),orders=read('web/app-orders.js'),catalog=read('web/app-catalog.js'),sw=read('web/sw.js'),orderCore=read('web/app-order-core.js'),master=read('web/app-master-order.js'),invoiceEntry=read('web/app-invoice-entry-v29.js'),telemetry=read('web/app-telemetry-v13.js'),documents=read('web/app-documents.js'),payments=read('web/app-payment-workflow.js');
assert.equal((app.match(/window\.fetch\s*=/g)||[]).length,0,'app.js must not wrap global fetch');
assert.ok(entry.includes("from './app-orders.js'")&&entry.includes("from './app-catalog.js'"),'professional bootstrap must use semantic modules');
assert.ok(bootstrap.includes("import('./app-professional.js')")&&!bootstrap.includes("from './app-professional.js'")&&app.includes('await initializeAuthenticatedPlatform()'),'professional graph must be deferred until authentication succeeds');
assert.ok(runtime.includes("from './app-operations-dashboard.js'")&&runtime.includes("registerRouteRenderer('dashboard'")&&runtime.includes("registerRouteRenderer('operations'"),'canonical runtime must own dashboard and operations through semantic modules');
assert.ok(bootstrap.includes("from './app-order-core.js'")&&bootstrap.includes('initializeOrderCore()')&&!bootstrap.includes('app-order-core-v15.js'),'bootstrap must load one canonical master-list entry');
assert.ok(orderCore.includes("import('./app-master-order.js')")&&!orderCore.includes('groupProductsByConfiguredOrder'),'master order implementation must load lazily behind its canonical entry');
assert.ok(!bootstrap.includes("app-master-v18.js")&&!bootstrap.includes('initializeMasterV18'),'legacy master enhancer must not run');
assert.ok(!invoiceEntry.includes("app-orders-v30.js")&&!invoiceEntry.includes('initializeOrdersV30'),'legacy order renderer must never register the orders route');
assert.ok(!files.includes('IntersectionObserver')&&!files.includes('MutationObserver'),'sharing must be explicit and must not scan DOM from the file layer');
assert.ok(orders.includes("new Set(['closed','cancelled'])")&&orders.includes("view:history?'history':'active'")&&orders.includes('TRAZABILIDAD CERRADA'),'Pedidos and Historial must be separate canonical operational ledgers');
assert.ok(orders.includes('data-v67-order-actions')&&!orders.includes('data-v43-order-menu'),'order actions must be native and canonical');
assert.equal((catalog.match(/data-v32-photo=/g)||[]).length,1,'product card must expose one photo action');
assert.ok(catalog.includes('data-v32-matrix-product')&&catalog.includes("/suppliers`,{method:'PUT'"),'supplier matrix must enable and disable product-supplier links directly');
assert.ok((mobile.match(/MutationObserver/g)||[]).length<=1&&mobile.includes("modalObserver.observe(body,{subtree:true,childList:true})"),'mobile observer must be modal-scoped');
assert.ok(!mobile.includes('applyConfiguredMasterOrder'),'mobile runtime must not reparent master rows');
assert.ok(mobile.includes('unit.tabIndex=-1')&&mobile.includes('supplier.tabIndex=-1'),'keyboard accessory traversal must skip format and supplier controls');
assert.ok(mobile.includes('keepQuantityVisible')&&mobile.includes("body.addEventListener('focusin'")&&mobile.includes('scrollIntoView'),'arrow navigation must keep the next quantity visible while the keyboard is open');
assert.ok(mobile.includes('.order-file-supplier{display:block!important')&&mobile.includes('max-width:150px!important'),'master list must show a compact supplier identity');
assert.ok(mobile.includes('.order-file-category>.order-file-row:nth-of-type(even)'),'master rows must use alternating theme-aware tones');
assert.ok(mobile.includes('.v44-favorite,[data-v44-master-mode="favorites"]{display:none!important'),'favorite controls must not consume master-list space');
assert.ok(master.includes('groupProductsByConfiguredOrder(visible,activeConfig)'),'master list must be ordered before DOM render');
assert.ok(master.includes('LOCAL_DRAFT_VERSION=71')&&master.includes('SERVER_AUTOSAVE_DELAY=1200')&&master.includes('idempotencyKey')&&master.includes("api('/api/autosave'")&&master.includes('Guardado en sistema · disponible en otros dispositivos'),'master list must protect locally and synchronize a retry-safe draft to the system');
assert.ok(master.includes('id="clearMasterQuantities"')&&master.includes("for(const value of selections.values())value.quantity=''"), 'master list must clear all quantity inputs without recapturing stale DOM values');
assert.ok(master.includes('Formato: <b>')&&master.includes('openMasterOrder({order:'),'create and edit must share the same master order renderer with visible purchase format');
assert.ok(runtime.includes('.v40-dashboard')&&runtime.includes('.v40-chart-line'),'dashboard visual system must be loaded by canonical runtime');
assert.ok(sw.includes('nuvasto-v77-payment-proof-ops')&&sw.includes('sessionCacheKey')&&sw.includes('apiDataResponse'),'service worker must provide authenticated offline data fallback and invalidate stale finance/catalog UI');
assert.ok(!sw.includes("'./app-procurement.js'")&&!sw.includes("'./app-enterprise.js'"),'service worker critical precache must exclude noncritical feature bundles');
assert.ok(sw.includes('RELEASE_ASSET')&&sw.includes('releaseAssetResponse')&&sw.includes('const cached=await cache.match'),'release JS/CSS must be cache-first inside a release-scoped cache');
assert.ok(documents.includes('minValue')&&documents.includes('meta.suppliers')&&documents.includes('data-v32-image-key'),'documents must expose preview and operational filters');
assert.ok(payments.includes('openPaymentForOrder')&&payments.includes('openPaymentDocumentComposer')&&payments.includes('openCollectivePayment')&&payments.includes('openPaymentMethodsManager')&&entry.includes('initializePaymentWorkflow()'),'orders, history and finance must converge on one semantic payment-document workflow');
assert.ok(app.includes('payment-collective')&&app.includes('openCollectivePayment'),'mobile More must expose direct collective payment access');
assert.ok(app.includes('controllerchange')&&app.includes("registration?.waiting?.postMessage?.({type:'SKIP_WAITING'})"),'client must force a clean service-worker release cutover');
assert.ok(telemetry.includes("window.addEventListener('nuvasto:api-metric'")&&telemetry.includes('requestBudget')&&telemetry.includes('DAY_LIMIT=100000'),'request budgets must be measured without adding per-request telemetry calls');
console.log('canonical runtime regression gate: OK');
