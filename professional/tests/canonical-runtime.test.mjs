import assert from 'node:assert/strict';import fs from 'node:fs';const read=path=>fs.readFileSync(path,'utf8');
const app=read('web/app.js'),entry=read('web/app-professional.js'),runtime=read('web/app-runtime.js'),mobile=read('web/app-mobile-runtime.js'),bootstrap=read('web/app-bootstrap.js'),files=read('web/app-file-actions.js'),orders=read('web/app-orders.js'),catalog=read('web/app-catalog.js'),sw=read('web/sw.js'),orderCore=read('web/app-order-core-v15.js');
assert.equal((app.match(/window\.fetch\s*=/g)||[]).length,0,'app.js must not wrap global fetch');
assert.ok(entry.includes("from './app-orders.js'")&&entry.includes("from './app-catalog.js'"),'professional bootstrap must use semantic modules');
assert.ok(runtime.includes("from './app-operations-dashboard.js'")&&runtime.includes("registerRouteRenderer('dashboard'")&&runtime.includes("registerRouteRenderer('operations'"),'canonical runtime must own dashboard and operations through semantic modules');
assert.ok(!bootstrap.includes("app-master-v18.js")&&!bootstrap.includes('initializeMasterV18'),'legacy master enhancer must not run');
assert.ok(!files.includes('IntersectionObserver')&&!files.includes('MutationObserver'),'sharing must be explicit and must not scan DOM');
assert.ok(orders.includes("order.status==='draft'")&&orders.includes("order.status!=='draft'&&order.status!=='cancelled'"),'orders and history must be separated at source');assert.ok(orders.includes('data-v57-order-actions')&&!orders.includes('data-v43-order-menu'),'order actions must be native and canonical');
assert.equal((catalog.match(/data-v32-photo=/g)||[]).length,1,'product card must expose one photo action');assert.ok((mobile.match(/MutationObserver/g)||[]).length<=1&&mobile.includes("modalObserver.observe(body,{subtree:true,childList:true})"),'mobile observer must be modal-scoped');assert.ok(!mobile.includes('applyConfiguredMasterOrder'),'mobile runtime must not reparent master rows');assert.ok(mobile.includes('unit.tabIndex=-1')&&mobile.includes('supplier.tabIndex=-1'),'keyboard accessory traversal must skip format and supplier controls');assert.ok(mobile.includes('.order-file-supplier{display:block!important')&&mobile.includes('.order-file-supplier-static{display:flex!important'),'master list must show supplier identity for single and multiple suppliers');assert.ok(mobile.includes('.order-file-quantity{width:64px!important'),'quantity input must remain compact');assert.ok(mobile.includes('.v44-favorite,[data-v44-master-mode="favorites"]{display:none!important'),'favorite controls must not consume master-list space');assert.ok(orderCore.includes('groupProductsByConfiguredOrder(visible,activeConfig)'),'master list must be ordered before DOM render');assert.ok(runtime.includes('.v40-dashboard')&&runtime.includes('.v40-chart-line'),'dashboard visual system must be loaded by canonical runtime');assert.ok(sw.includes('nuvasto-v64-offline-data')&&sw.includes('sessionCacheKey')&&sw.includes('apiDataResponse'),'service worker must provide authenticated offline data fallback');console.log('canonical runtime regression gate: OK');
